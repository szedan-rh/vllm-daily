# vLLM Merged PR Report

**Report Date:** 2026-07-14 PDT

**Total Merged PRs:** 39

---

## 1. [[ROCm][CI] fix test_common.py](https://github.com/vllm-project/vllm/pull/48676)


### Base Information

- **PR Number:** #48676
- **Author:** [charlifu](https://github.com/charlifu)
- **Merged By:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged time:** 2026-07-14 23:42:07
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48676/files) (2):**
  - `tests/conftest.py`
  - `vllm/model_executor/models/hyperclovax.py`

### Summary

**What changed and why**  
Fixed test_common.py failures caused by newer Transformers requiring a tokenizer when generate() uses stop strings. HfRunner now passes the tokenizer by default in all three model.generate() paths, and a default parameter was added to hyperclovax.py's forward method to avoid a missing argument error.

**Technical impact**  
Ensures CI tests pass with updated Transformers dependency. The tokenizer is now automatically passed to generate() unless overridden, preventing ValueError from stop strings. The hyperclovax model forward signature fix prevents TypeError when intermediate_tensors is not provided.

**Potential risks**  
Low risk. The tokenizer default is set via setdefault, so explicit overrides still work. The hyperclovax change is backward-compatible as it only adds a default value. No behavioral changes beyond fixing the errors.

**Key insights**  
The root cause was a new Transformers requirement for tokenizer in generate() with stop strings. The fix uses setdefault to maintain backward compatibility. The hyperclovax change was a minor signature mismatch that would cause runtime errors.

---

## 2. [[ROCm] Run init test engine in-process to avoid KV-cache OOM](https://github.com/vllm-project/vllm/pull/48527)


### Base Information

- **PR Number:** #48527
- **Author:** [djramic](https://github.com/djramic)
- **Merged By:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged time:** 2026-07-14 23:39:31
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48527/files) (1):**
  - `tests/models/test_initialization.py`

### Summary

**What changed and why**  
In test_initialization.py, added a conditional environment variable override to disable V1 multiprocessing on spawn platforms. This ensures the KV-cache size patch applied to EngineCore._initialize_kv_caches is not lost when the engine runs in a subprocess, preventing OOM errors during tests.

**Technical impact**  
Fixes a test failure on spawn multiprocessing platforms (e.g., ROCm) where the KV-cache cap patch was not inherited by the engine subprocess, causing out-of-memory errors. The change forces in-process execution only for these platforms, preserving the patch's effect.

**Potential risks**  
Low risk. The change is scoped to test code and only activates on spawn platforms. It may slightly alter test isolation by running the engine in-process, but the test already uses patches and monkeypatching, so the impact is minimal.

**Key insights**  
The root cause is that spawn multiprocessing re-imports modules, losing monkeypatches. The fix uses an existing utility to detect spawn platforms and disables multiprocessing for the test, ensuring the patch applies. This is a targeted workaround rather than a general solution.

---

## 3. [[Bugfix][Spec Decode] Support heterogeneous QK fusion geometry](https://github.com/vllm-project/vllm/pull/48671)


### Base Information

- **PR Number:** #48671
- **Author:** [aoshen02](https://github.com/aoshen02)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-07-14 22:37:11
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48671/files) (2):**
  - `tests/compile/passes/test_qk_norm_rope_fusion.py`
  - `vllm/compilation/passes/fusion/qk_norm_rope_fusion.py`

### Summary

**What changed and why**  
The QK Norm+RoPE fusion pass previously used only the first attention layer's geometry, causing failures when speculative decoding uses target and draft models with different head counts. The fix collects all unique (head_dim, num_heads, num_kv_heads) geometries from the attention registry and registers patterns for each, so both target and draft graphs can fuse correctly.

**Technical impact**  
Enables QK Norm+RoPE fusion for heterogeneous speculative decoding setups, fixing a ValueError during compilation. The pass now handles multiple attention geometries, and the UUID includes the geometry set to prevent incorrect cache reuse. A regression test verifies compilation of both a 32-head target and a 16-head draft from the same forward context.

**Potential risks**  
Increased pattern registration may slightly increase compilation time. The change relies on converting ValueError to RuntimeError for pattern mismatch handling, which could affect other error scenarios. The fix was validated on NVIDIA H200 but not on AMD hardware, though the platform check is shared.

**Key insights**  
The root cause was assuming a single attention geometry in speculative decoding contexts. The solution generalizes pattern registration to all geometries and uses exception conversion to gracefully skip incompatible patterns. Including geometry in the pass UUID prevents stale cache entries.

---

## 4. [[Bugfix] Set kv_quant_mode on the generic MLA KV-cache spec](https://github.com/vllm-project/vllm/pull/48379)


### Base Information

- **PR Number:** #48379
- **Author:** [drakosha](https://github.com/drakosha)
- **Merged By:** [MatthewBonanni](https://github.com/MatthewBonanni)
- **Merged time:** 2026-07-14 20:36:53
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48379/files) (1):**
  - `vllm/model_executor/layers/attention/mla_attention.py`

### Summary

**What changed and why**  
Added `kv_quant_mode=get_kv_quant_mode(self.kv_cache_dtype)` to the `MLAAttentionSpec` constructor in `MLAAttention.get_kv_cache_spec`. Previously, `kv_quant_mode` was left at its default `KVQuantMode.NONE`, causing a shape mismatch in the KV-cache reshape for MLA layers when using `fp8_ds_mla` dtype. This mirrors a fix already applied to the DeepseekV4-specific spec constructor.

**Technical impact**  
Fixes a runtime crash (`RuntimeError: shape '[512, 64, 576]' is invalid for input of size 21495808`) during engine initialization for DeepSeek-V3.2 / GLM DSA models with `--kv-cache-dtype fp8_ds_mla`. The fix ensures the KV-cache spec correctly reflects the quantization mode, allowing proper tensor reshaping and allocation.

**Potential risks**  
Low risk. The change is a one-line addition that sets a field that was previously unset (defaulting to NONE). It aligns the generic MLA path with the already-fixed DeepseekV4 path. The fix has been validated with multiple test scenarios including TP, DCP, EP, and MTP at large context lengths.

**Key insights**  
The bug was caused by an oversight in the generic MLA attention spec constructor, which did not propagate the `kv_quant_mode` from the cache dtype. This highlights the importance of ensuring all code paths that construct attention specs handle quantization mode consistently, especially when multiple specialized paths exist (e.g., DeepseekV4 vs generic MLA).

---

## 5. [Build with ABI stable FlashMLA](https://github.com/vllm-project/vllm/pull/48174)


### Base Information

- **PR Number:** #48174
- **Author:** [janeyx99](https://github.com/janeyx99)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-07-14 20:29:29
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48174/files) (1):**
  - `cmake/external_projects/flashmla.cmake`

### Summary

**What changed and why**  
Updated the FlashMLA git tag to a newer commit that includes ABI-stable changes (PRs #15 and #16). Modified the build system to remove Py_LIMITED_API undefines and instead define TORCH_TARGET_VERSION and USE_CUDA for ABI stability. Also updated source file paths and include directories to match the new FlashMLA structure.

**Technical impact**  
The FlashMLA extension is now built with ABI stability for torch 2.11+, which improves compatibility and reduces the risk of ABI mismatches. The change also updates the source code to use the new stable API (e.g., replacing 'import flash_mla.cuda as flash_mla_cuda' with 'flash_mla_cuda = torch.ops._flashmla_C').

**Potential risks**  
The new FlashMLA commit may introduce behavioral changes or regressions. The build system changes (removing Py_LIMITED_API undefines) could affect compilation on older torch versions. The updated include paths and source files must be correct for all supported architectures.

**Key insights**  
This PR moves to an ABI-stable version of FlashMLA, which is a significant step for long-term maintainability. The test results show all tests pass on supported hardware, but dense tests on H100 were skipped due to hardware limitations. The change is part of a larger effort to stabilize the FlashMLA integration.

---

## 6. [[Security] Replace diskcache to eliminate pickle deserialization](https://github.com/vllm-project/vllm/pull/44549)


### Base Information

- **PR Number:** #44549
- **Author:** [russellb](https://github.com/russellb)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-07-14 20:29:24
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44549/files) (7):**
  - `requirements/common.txt`
  - `requirements/test/cpu.txt`
  - `requirements/test/cuda.txt`
  - `requirements/test/rocm.txt`
  - `requirements/test/xpu.txt`
  - `tests/v1/structured_output/test_outlines_cache.py`
  - `vllm/v1/structured_output/utils.py`

### Summary

**What changed and why**  
Replaced diskcache (which uses pickle) with a custom SQLite-backed cache (OutlinesDiskCache) to eliminate the risk of arbitrary code execution via pickle deserialization. The change addresses CVE-2025-69872 and removes the diskcache dependency from requirements files. Added comprehensive unit tests for the new cache implementation.

**Technical impact**  
The outlines structured output cache now uses SQLite with outlines_core's native binary serialization (via Index.from_binary) instead of pickle. The API remains dict-like, so existing code using the cache requires no changes. The diskcache dependency is removed, which may affect environments that relied on it for other purposes. The cache is still disabled by default and gated behind VLLM_V1_USE_OUTLINES_CACHE.

**Potential risks**  
The new cache is a custom implementation and may have edge cases not covered by diskcache's mature codebase. SQLite concurrency with check_same_thread=False could lead to issues if multiple threads access the cache simultaneously. The cache uses WAL mode for performance, but SQLite may have different performance characteristics than diskcache. The change removes diskcache entirely, which could break any code that imported it directly (though it was only used internally).

**Key insights**  
The PR eliminates a critical security vulnerability (CVSS 9.8) by removing pickle deserialization. The new cache leverages outlines_core's existing binary serialization, which is safer and more efficient. The test coverage is thorough, including persistence, version invalidation, and edge cases. The change is well-documented with clear comments and a security-focused commit message.

---

## 7. [[LoRA][1/N] Integrate flashinfer MoE LoRA for BF16 model](https://github.com/vllm-project/vllm/pull/48632)


### Base Information

- **PR Number:** #48632
- **Author:** [jeejeelee](https://github.com/jeejeelee)
- **Merged By:** [jeejeelee](https://github.com/jeejeelee)
- **Merged time:** 2026-07-14 19:54:00
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48632/files) (2):**
  - `vllm/model_executor/layers/fused_moe/experts/trtllm_lora_moe.py`
  - `vllm/model_executor/layers/fused_moe/oracle/unquantized.py`

### Summary

**What changed and why**  
Added a new LoRA-aware MoE experts implementation (TrtLlmBf16LoRAExperts) that integrates FlashInfer's TRT-LLM fused MoE kernel with LoRA support for BF16 models. The change includes a new file (trtllm_lora_moe.py) with the experts class and modifications to the oracle (unquantized.py) to route LoRA-enabled BF16 MoE to this new backend when device and configuration constraints are met. This is part of a larger effort to support LoRA in vLLM's MoE layers.

**Technical impact**  
Introduces a new code path for MoE with LoRA that leverages FlashInfer's routed kernel with gemm1_lora_delta for fusing W13 LoRA deltas into the FC1 computation, while W2 LoRA is computed out-of-kernel via punica. The implementation is restricted to SM100+ (Blackwell), BF16, gated SwiGLU, shuffled weights, and requires intermediate size per partition to be a multiple of 128. The oracle now selects this backend when conditions are met, otherwise falls back to Triton.

**Potential risks**  
The new backend is highly specialized and may not be compatible with all configurations (e.g., non-BF16, non-gated activations, non-shuffled weights, or intermediate sizes not multiple of 128). The code includes assertions and conditional checks, but runtime failures could occur if assumptions are violated. The LoRA delta order swap (gate/up halves) is a potential source of correctness issues if the kernel's expected order changes. The use of meta tensors for shape inference may mask shape mismatches until runtime.

**Key insights**  
The implementation carefully handles the mismatch between vLLM's w13 order (gate first) and FlashInfer's expected order (up first) by swapping halves. The W2 LoRA is handled out-of-kernel due to kernel limitations, requiring unpermutation of activation output. The workspace shapes are zeroed out because FlashInfer manages its own memory. The code includes extensive documentation and constraints matching the upstream FlashInfer PR #3153.

---

## 8. [[Quant] Enable humming w[2-7]a[4,8] inference with compressed-tensors](https://github.com/vllm-project/vllm/pull/46390)


### Base Information

- **PR Number:** #46390
- **Author:** [HDCharles](https://github.com/HDCharles)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-07-14 19:22:31
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/46390/files) (8):**
  - `tests/evals/gsm8k/configs/humming/Qwen3-4B-mixed-quant-RTN-humming.yaml`
  - `tests/evals/gsm8k/configs/humming/config-act-int8.txt`
  - `vllm/model_executor/kernels/linear/mixed_precision/humming.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/compressed_tensors.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/schemes/__init__.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_wNa4.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_wNa8.py`
  - `vllm/model_executor/layers/quantization/utils/humming_utils.py`

### Summary

**What changed and why**  
Added support for compressed-tensors quantized models with INT weight bits (2-8) and INT4 or INT8 symmetric dynamic activation quantization using the Humming linear kernel. This extends existing wNa8o8 support to cover vanilla wNa4 and wNa8 schemes, as discussed in PR #45185. New scheme classes CompressedTensorsWNA4Int and CompressedTensorsWNA8Int were added, along with detection logic in compressed_tensors.py and a test config.

**Technical impact**  
Enables inference of models quantized with compressed-tensors using weight-only INT quantization (2-8 bits) and INT4 or INT8 activation quantization via the Humming backend. The change modifies the weight processing pipeline to pass an input quantization config to prepare_humming_layer, allowing the kernel to handle dynamic per-token/per-group activation quantization. New scheme classes follow the existing pattern and reuse WNA16_SUPPORTED_TYPES_MAP for weight type mapping.

**Potential risks**  
The new schemes are only supported on compute capability >= 7.5 (sm75). The input quantization config is passed via a new attribute _humming_input_quant_config, which could conflict with other quantization methods if not properly isolated. The detection logic in _is_wNaM_int may incorrectly match some configurations, though the checks are fairly specific. The test coverage is limited to a single model config and an e2e test referenced externally.

**Key insights**  
The implementation separates wNa4 and wNa8 schemes from the existing wNa8o8 scheme, which had both input and output quantization. The new schemes only quantize the input activation, not the output. The code is largely duplicated between wNa4 and wNa8 files, suggesting potential for future refactoring into a shared base class. The change also adds a test config for a Qwen3-4B model with mixed quantization.

---

## 9. [[CI][AMD] Configure MI300 tests for native execution without DinD](https://github.com/vllm-project/vllm/pull/48387)


### Base Information

- **PR Number:** #48387
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-07-14 19:14:17
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48387/files) (23):**
  - `.buildkite/lm-eval-harness/configs/Meta-Llama-4-Maverick-17B-128E-Instruct-FP8.yaml`
  - `.buildkite/lm-eval-harness/test_lm_eval_correctness.py`
  - `.buildkite/scripts/ci-bake-rocm.sh`
  - `.buildkite/scripts/hardware_ci/run-amd-test.sh`
  - `.buildkite/test-amd.yaml`
  - `.buildkite/test_areas/benchmarks.yaml`
  - `.buildkite/test_areas/disaggregated.yaml`
  - `.buildkite/test_areas/distributed.yaml`
  - `.buildkite/test_areas/engine.yaml`
  - `.buildkite/test_areas/expert_parallelism.yaml`
  - `.buildkite/test_areas/lm_eval.yaml`
  - `.buildkite/test_areas/misc.yaml`
  - `.buildkite/test_areas/model_executor.yaml`
  - `.buildkite/test_areas/models_language.yaml`
  - `.buildkite/test_areas/models_multimodal.yaml`
  - `.buildkite/test_areas/pytorch.yaml`
  - `.buildkite/test_areas/weight_loading.yaml`
  - `docker/Dockerfile.rocm`
  - `tests/basic_correctness/test_mem.py`
  - `tests/standalone_tests/python_only_compile.sh`
  - `tests/v1/kv_connector/nixl_integration/spec_decode_acceptance_test.sh`
  - `tests/v1/kv_connector/unit/test_mooncake_store_hma_e2e.py`
  - `vllm/model_executor/model_loader/weight_utils.py`

### Summary

**What changed and why**  
This PR configures MI300 tests to run natively on the Kubernetes pod without Docker-in-Docker (DinD). It adds a `dind: false` flag to all MI300 job definitions in `.buildkite/test-amd.yaml` and several test area YAML files, and implements the native execution path in `run-amd-test.sh` including workspace validation, artifact download with retries and checksum verification, and environment initialization. The wheel artifact upload in `ci-bake-rocm.sh` is hardened with strict cardinality checks, metadata files, and checksum generation. Additional changes include ROCm-specific memory limit awareness in `weight_utils.py`, a lazy safetensors load strategy for ROCm in lm-eval, and minor fixes in test scripts.

**Technical impact**  
MI300 CI jobs will now execute directly on the host pod instead of inside a Docker container, reducing overhead and improving resource utilization. The artifact pipeline becomes more robust with exact producer step, retries, checksum validation, and metadata tracking. The `_get_available_ram_bytes` function now respects cgroup memory limits on ROCm, preventing out-of-memory errors. The `dind: false` flag is configuration-driven, allowing other AMD devices to continue using legacy DinD.

**Potential risks**  
Native execution may expose host environment inconsistencies not seen in Docker, such as missing dependencies or permission issues. The strict artifact validation (exactly one wheel, commit/base image matching) could cause failures if the build step produces unexpected artifacts. The cgroup memory limit change might affect non-ROCm platforms if `get_cgroup_memory_limit` is not available. The `dind: false` flag is added to many jobs; if the pod contract is not updated accordingly, jobs may fail to start.

**Key insights**  
The PR introduces a clear separation between native and DinD execution paths via the `dind` flag, making the CI system more flexible. The artifact contract is significantly hardened with metadata and checksums, improving reliability. The memory limit fix for ROCm addresses a real issue where cgroup limits were ignored. The changes are well-structured with validation steps and fallbacks, reducing the risk of regressions.

---

## 10. [[Core][LoRA] Support fp32 lm_head (head_dtype) on the LoRA path](https://github.com/vllm-project/vllm/pull/48525)


### Base Information

- **PR Number:** #48525
- **Author:** [KKothuri](https://github.com/KKothuri)
- **Merged By:** [jeejeelee](https://github.com/jeejeelee)
- **Merged time:** 2026-07-14 18:51:09
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48525/files) (3):**
  - `tests/lora/test_layers.py`
  - `tests/v1/sample/test_head_dtype.py`
  - `vllm/lora/layers/logits_processor.py`

### Summary

**What changed and why**  
Removed the guard that rejected head_dtype != model dtype when LoRA is enabled, and modified LogitsProcessorWithLoRA._get_logits to use LogitsProcessor._apply_head for the base projection so that an fp32 lm_head is honored on the LoRA path. This enables RL + LoRA workflows that require fp32 lm_head.

**Technical impact**  
The LoRA logits path now respects head_dtype (e.g., fp32) by reusing the existing _apply_head method from the base LogitsProcessor. No kernel changes are needed because add_lora_logits already works in fp32 and handles mixed-precision accumulation. The change is minimal (+11/-29 lines) and focused on the logits processor.

**Potential risks**  
Low risk. The change is well-scoped and tested. Quantized lm_head + fp32 head remains unsupported (unchanged). The removed guard could have silently caused incorrect behavior if head_dtype was set incorrectly, but now it will work correctly. The test suite covers the new path with 18 passed tests.

**Key insights**  
The fix leverages existing infrastructure (_apply_head) rather than duplicating logic. The LoRA delta accumulation was already fp32-safe, so only the base projection needed adjustment. This is a clean follow-up to PR #48390 that unblocks a requested use case.

---

## 11. [[ROCm][CI] Remove mxfp4 test skips after `amd-quark` 0.12 release](https://github.com/vllm-project/vllm/pull/47330)


### Base Information

- **PR Number:** #47330
- **Author:** [micah-wil](https://github.com/micah-wil)
- **Merged By:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged time:** 2026-07-14 18:25:06
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/47330/files) (5):**
  - `requirements/rocm.txt`
  - `tests/evals/gsm8k/test_gsm8k_correctness.py`
  - `tests/kernels/moe/test_ocp_mx_moe.py`
  - `tests/lora/test_gptoss_tp.py`
  - `tests/quantization/test_quark.py`

### Summary

**What changed and why**  
Bumped amd-quark dependency from >=0.8.99 to ==0.12.post1 to unblock MXFP4 functionality broken with Torch 2.11. Removed version compatibility checks and test skips that were temporary workarounds for older quark versions. Also adjusted test skip conditions for MXFP4 tests to rely on platform availability instead of quark version, and fixed a whitespace comparison in LoRA test.

**Technical impact**  
Enables MXFP4 quantization tests on ROCm with Torch 2.11+ by ensuring a compatible amd-quark version. Removes conditional test skips, increasing test coverage. The dependency pin to a specific version may require updates when newer quark releases are needed.

**Potential risks**  
Pinning amd-quark to 0.12.post1 could cause conflicts if other components require a different version. The removal of version checks assumes all environments will have this exact version, which might break CI if the package is not available. The LoRA test whitespace change could mask subtle differences in output formatting.

**Key insights**  
The PR cleans up temporary workarounds after the upstream quark fix. The dependency pin is a pragmatic choice to ensure consistency, but long-term a range might be better. The test modifications show a shift from runtime version checks to relying on environment setup, which simplifies code but reduces flexibility.

---

## 12. [add pad-aware reduce path](https://github.com/vllm-project/vllm/pull/48385)


### Base Information

- **PR Number:** #48385
- **Author:** [gnovack](https://github.com/gnovack)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-07-14 18:05:51
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48385/files) (7):**
  - `csrc/libtorch_stable/dispatch_utils.h`
  - `csrc/libtorch_stable/moe/moe_align_sum_kernels.cu`
  - `csrc/libtorch_stable/moe/moe_ops.h`
  - `csrc/libtorch_stable/moe/torch_bindings.cpp`
  - `tests/kernels/moe/test_moe.py`
  - `vllm/_custom_ops.py`
  - `vllm/model_executor/layers/fused_moe/experts/marlin_moe.py`

### Summary

**What changed and why**  
Added a padding-aware reduce path for Marlin MoE to skip padding tokens when expert parallelism is enabled. Previously, the moe_sum kernel iterated over all tokens including padding, and the output buffer was zeroed each invocation. This change introduces optional topk_ids and expert_map parameters to the moe_sum function, enabling the kernel to skip unrouted or non-local expert slots, and removes the zeroing of the output buffer when expert_map is provided.

**Technical impact**  
Performance improvement for MoE with expert parallelism, as shown by benchmark results (e.g., median TPOT reduced from 25.06ms to 19.61ms at concurrency 16). The change modifies the moe_sum kernel signatures to accept index tensors and adds dispatch macros for index types. It also updates the Python bindings and the fused_marlin_moe function to pass the new parameters. The test suite includes a new test for the pad-aware reduce path.

**Potential risks**  
The pad-aware path is only enabled when topk_ids and expert_map are provided; existing callers without these parameters retain the original behavior. However, the removal of output.zero_() when expert_map is not None could cause issues if other code relies on the buffer being zeroed. The new kernel uses a device function to check padding, which may have a slight overhead for non-padded tokens. The change also introduces new template parameters and dispatch logic, increasing code complexity and potential for compilation errors.

**Key insights**  
The optimization leverages the fact that padding tokens are marked with negative expert IDs in topk_ids or negative entries in expert_map, allowing the kernel to skip them efficiently. The benchmark shows significant throughput gains at lower concurrency, with minimal regression at higher concurrency. The change is backward-compatible as the new parameters are optional.

---

## 13. [[Perf] Remove redundant repeat and copy for dsv4, 1.8% E2E TPOT improvement.](https://github.com/vllm-project/vllm/pull/48137)


### Base Information

- **PR Number:** #48137
- **Author:** [yewentao256](https://github.com/yewentao256)
- **Merged By:** [yewentao256](https://github.com/yewentao256)
- **Merged time:** 2026-07-14 17:48:10
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48137/files) (3):**
  - `vllm/model_executor/kernels/mhc/tilelang.py`
  - `vllm/model_executor/kernels/mhc/tilelang_kernels.py`
  - `vllm/models/deepseek_v4/nvidia/model.py`

### Summary

**What changed and why**  
Removed redundant unsqueeze and repeat operations for the first decode step in DeepSeek-V4 by introducing a broadcast path. The original code expanded hidden_states from (T, H) to (T, hc_mult, H) via repeat, which was wasteful. The new mhc_pre_broadcast_tilelang kernel fuses the broadcast with the existing mHC pre computation, reducing memory and compute overhead.

**Technical impact**  
Achieves 1.8% E2E TPOT improvement (9.70ms -> 9.52ms) by eliminating unnecessary data expansion and copy. The change adds a new TileLang kernel (mhc_pre_big_fuse_broadcast_with_norm_tilelang) and a Python wrapper (mhc_pre_broadcast_tilelang) that operates on 2D residual input directly, computing the broadcast implicitly during the fused GEMM and normalization. The model's forward method now checks tensor dimensionality to select the optimized path.

**Potential risks**  
The new kernel is only used when residual is 2D (first layer with no previous residual). If the tensor dimensionality changes unexpectedly, the fallback to the original path ensures correctness. The broadcast weight (fn_broadcast) is computed by summing over the hc_mult dimension of the original fn weight, which must be mathematically equivalent to the original repeat+multiply. Accuracy is validated via GSM8K benchmark (0.9568 vs 0.9575, within noise).

**Key insights**  
The optimization leverages the fact that the repeat operation is redundant when the subsequent computation is linear in the repeated dimension. By pre-summing the weight matrix along the repeat dimension, the broadcast can be fused into the GEMM, reducing memory bandwidth and compute. This is a common pattern in transformer optimization where tensor expansions can be eliminated by reordering operations.

---

## 14. [[Bugfix] Preserve unloaded non-persistent buffers during layerwise reload](https://github.com/vllm-project/vllm/pull/44371)


### Base Information

- **PR Number:** #44371
- **Author:** [joanvelja](https://github.com/joanvelja)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-07-14 17:46:29
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44371/files) (4):**
  - `tests/model_executor/model_loader/test_reload.py`
  - `vllm/model_executor/model_loader/reload/layerwise.py`
  - `vllm/model_executor/model_loader/reload/meta.py`
  - `vllm/model_executor/model_loader/reload/types.py`

### Summary

**What changed and why**  
Fixed a bug where layerwise reload corrupted unloaded non-persistent direct buffers (e.g., Gemma4 router root_size) by preserving their metadata and skipping copy-back for buffers whose weight loader never ran. Added snapshot of non-persistent buffer set before restore, and used it to maintain persistence flag and avoid overwriting unloaded buffers.

**Technical impact**  
Ensures correctness of non-persistent buffers during layerwise reload, preventing silent model divergence (e.g., KL jump from 0.01 to >1.5). Adds 4 new regression tests and modifies 3 core reload modules to track and preserve non-persistent buffer state.

**Potential risks**  
Low risk: changes are narrow (non-persistent buffer handling only) and covered by new tests. Existing alias-buffer tests pass. Edge case: if a non-persistent buffer is intentionally loaded, it is still updated correctly.

**Key insights**  
The bug occurred because copy-back overwrote unloaded non-persistent buffers with meta-tensor sentinel values. The fix snapshots the non-persistent set before restore and skips copy-back for unloaded buffers, while preserving the persistent flag when re-registering buffers.

---

## 15. [[Bugfix][R3] Exclude draft routers from expert capture](https://github.com/vllm-project/vllm/pull/48622)


### Base Information

- **PR Number:** #48622
- **Author:** [aoshen02](https://github.com/aoshen02)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-07-14 17:45:35
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48622/files) (2):**
  - `tests/model_executor/test_routed_experts_capture.py`
  - `vllm/v1/worker/gpu_model_runner.py`

### Summary

**What changed and why**  
Changed `_bind_routed_experts_capturer` to iterate over `self.model.modules()` instead of `self.compilation_config.static_forward_context.values()`. This prevents draft model MoE modules (e.g., MTP) from overwriting target MoE capture hooks when both share the same `layer_id`, fixing a corruption bug in routed-experts (R3) capture during speculative decoding.

**Technical impact**  
Fixes a correctness bug where R3 replay could use draft expert IDs for target layers, causing wrong expert selection during training. The fix ensures only target model routers are bound to the capture callback, aligning with SGLang's approach. A regression test was added to verify that draft routers are not captured.

**Potential risks**  
Low risk. The change is minimal (one line) and only affects the iteration source. If a model has MoE modules outside `self.model` that were previously captured, they will no longer be captured, but such modules should not exist in the target model. The test covers the draft collision scenario.

**Key insights**  
The root cause was that `static_forward_context` includes both target and draft model modules, and MoE layers with the same `layer_id` (e.g., 0) collide. Iterating over `self.model.modules()` naturally excludes draft modules. The fix is simple and well-tested with both unit and end-to-end validation.

---

## 16. [fix: size FlashInfer prefill workspace to batch head footprint](https://github.com/vllm-project/vllm/pull/48428)


### Base Information

- **PR Number:** #48428
- **Author:** [joerowell](https://github.com/joerowell)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-07-14 17:18:41
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48428/files) (1):**
  - `vllm/v1/attention/backends/flashinfer.py`

### Summary

**What changed and why**  
Fixed the FlashInfer prefill workspace buffer size calculation to account for the batch head footprint (max_num_batched_tokens * num_qo_heads * head_dim * 16 bytes) instead of using a fixed default that overflows for wide-head models at large prefill chunks on certain architectures (e.g., sm_120). The change ensures the workspace is large enough to avoid hard errors from FlashInfer's batch_prefill_tmp_v.

**Technical impact**  
The workspace buffer size is now dynamically computed based on the model's head dimensions and maximum batched tokens, with a floor at the configured default. This prevents out-of-bounds errors during prefill for wide-head models, improving robustness without significantly increasing memory usage for typical configurations.

**Potential risks**  
The new calculation may increase memory usage for models with many heads or large head dimensions, but it is bounded by the configured default and should not exceed reasonable limits. There is a low risk of regression if the estimation formula is incorrect for some edge cases, but the change is straightforward and based on FlashInfer's internal buffer requirements.

**Key insights**  
The fix addresses a silent failure mode where FlashInfer's prefill workspace was undersized for wide-head models, causing hard errors on certain GPU architectures. The solution sizes the workspace to the maximum possible prefill chunk footprint, ensuring compatibility without relying on FlashInfer's internal buffer growth mechanism.

---

## 17. [[Bugfix][CI] Fix test_head_dtype quant_method test on ROCm](https://github.com/vllm-project/vllm/pull/48654)


### Base Information

- **PR Number:** #48654
- **Author:** [micah-wil](https://github.com/micah-wil)
- **Merged By:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged time:** 2026-07-14 16:31:36
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48654/files) (1):**
  - `tests/v1/sample/test_head_dtype.py`

### Summary

**What changed and why**  
The test `test_head_dtype_equal_to_model_dtype_uses_quant_method` was failing on ROCm because it used CPU tensors with a custom ROCm kernel (`rocm_unquantized_gemm`) that is not registered for the CPU backend. The fix patches `quant_method.apply` with a plain `F.linear` and asserts it was called, verifying the routing logic without relying on GPU-specific kernels.

**Technical impact**  
The test now passes on ROCm CI by mocking the quant_method.apply to use F.linear, which works on CPU. This ensures the test validates the correct code path (LogitsProcessor routing) without depending on GPU kernel availability.

**Potential risks**  
The mock replaces the actual quant_method.apply, so the test no longer exercises the real kernel dispatch. However, the test's purpose is to verify routing logic, not kernel correctness, so this is acceptable. The assertion ensures the mock was called, confirming the intended code path.

**Key insights**  
The failure occurred because the test used CPU tensors but the code path invoked a ROCm-specific custom op not available on CPU. The fix uses mocking to isolate the routing logic from kernel dependencies, a common pattern for testing platform-specific code paths.

---

## 18. [[Bugfix] MoRIIO toy P/D proxy: add /health](https://github.com/vllm-project/vllm/pull/45222)


### Base Information

- **PR Number:** #45222
- **Author:** [chaeminlim-mb](https://github.com/chaeminlim-mb)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-07-14 15:56:47
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/45222/files) (2):**
  - `examples/disaggregated/disaggregated_serving/moriio_toy_proxy_server.py`
  - `tests/v1/kv_connector/unit/test_moriio_toy_proxy_server.py`

### Summary

**What changed and why**  
Added a /health endpoint to the MoRIIO toy proxy server so that benchmark harnesses and load balancers can probe the proxy URL instead of getting a 404. Also fixed max_tokens decrement logic to respect max_completion_tokens when present. Added a corresponding unit test.

**Technical impact**  
The proxy now returns 200 on /health, preventing false dead-service detections. The max_tokens decrement now correctly handles max_completion_tokens, aligning with backend behavior. The test ensures the health route works as expected.

**Potential risks**  
Low risk. The health endpoint is trivial and the token fix is a minor logic correction. The test is gated to ROCm and uses runpy, which may have import side effects but is isolated.

**Key insights**  
The PR addresses a real operational gap where the proxy lacked a health endpoint, causing load balancers to mark it dead. The token fix was a necessary correction found during review. The test uses runpy to avoid import-time side effects, a pragmatic approach for example code.

---

## 19. [Log fully resolved pooling config at startup](https://github.com/vllm-project/vllm/pull/48030)


### Base Information

- **PR Number:** #48030
- **Author:** [taneem-ibrahim](https://github.com/taneem-ibrahim)
- **Merged By:** [yewentao256](https://github.com/yewentao256)
- **Merged time:** 2026-07-14 15:00:46
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48030/files) (3):**
  - `vllm/config/model.py`
  - `vllm/config/pooler.py`
  - `vllm/v1/engine/core.py`

### Summary

**What changed and why**  
Added logging of fully resolved pooling config at startup, including source attribution per field (user, sentence_transformers, model_default, pooler_default) and supported pooling tasks. This improves debuggability by showing exactly how the pooler config was derived.

**Technical impact**  
Introduces a new log line in the engine core that fires once per process when pooling is used. Adds a private attribute `_pooler_config_sources` to ModelConfig to track field origins. The logging is guarded by data parallel rank and runner type checks to avoid noise.

**Potential risks**  
Low risk. The change is additive logging with no behavioral impact on pooling logic. The new attribute is internal and not serialized. The log line is rate-limited via `logger.info_once`.

**Key insights**  
The PR addresses a common debugging pain point by making the pooling config resolution transparent. The source tracking pattern could be reused for other config resolution paths. The log format is designed to be parseable and informative.

---

## 20. [[CI][Bugfix] Fix FlashAttention reported MLA dimension support](https://github.com/vllm-project/vllm/pull/48631)


### Base Information

- **PR Number:** #48631
- **Author:** [MatthewBonanni](https://github.com/MatthewBonanni)
- **Merged By:** [MatthewBonanni](https://github.com/MatthewBonanni)
- **Merged time:** 2026-07-14 14:33:02
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48631/files) (5):**
  - `docs/design/attention_backends.md`
  - `tests/v1/attention/test_mla_prefill_selector.py`
  - `tools/pre_commit/generate_attention_backend_docs.py`
  - `vllm/v1/attention/backends/mla/prefill/base.py`
  - `vllm/v1/attention/backends/mla/prefill/flash_attn.py`

### Summary

**What changed and why**  
Fixed FlashAttention MLA dimension support reporting: FA2/FA3 support both DeepSeek (128,64,128) and GLM (192,64,256) dimensions, while FA4 only supports DeepSeek. Updated the FlashAttnPrefillBackend to override supports_mla_dimensions with version-aware logic, added a base class method for dimension checking, updated documentation, and enhanced the doc generation script to parse FA-version branches. This fixes CI failures for GLM models that were incorrectly rejected by FA4.

**Technical impact**  
Changes the behavior of MLA prefill backend selection for GLM-5 dimensions on Blackwell (FA4) systems: previously GLM dimensions would be incorrectly considered unsupported by FlashAttention, now they correctly fall back to TRTLLM. The base class now has a supports_mla_dimensions method that subclasses can override, improving extensibility. The doc generation script now produces more accurate documentation by parsing version-specific dimension support.

**Potential risks**  
Low risk. The change is well-tested with parametrized tests for both dimension sets. The new supports_mla_dimensions method in the base class defaults to checking supported_mla_dimensions, so existing subclasses without override remain unaffected. The doc generation changes are backward-compatible. The main risk is if other backends have similar version-dependent support that is not yet modeled, but that is out of scope.

**Key insights**  
The fix addresses a real CI failure caused by overly restrictive dimension checking. The approach of overriding supports_mla_dimensions in FlashAttnPrefillBackend is clean and allows version-specific logic without modifying the base class validation flow. The doc generation enhancement automatically captures version-dependent support, reducing manual documentation errors.

---

## 21. [[ROCm][CI] fix flashinfer import check](https://github.com/vllm-project/vllm/pull/48647)


### Base Information

- **PR Number:** #48647
- **Author:** [divakar-amd](https://github.com/divakar-amd)
- **Merged By:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged time:** 2026-07-14 13:54:20
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48647/files) (1):**
  - `tests/v1/attention/test_flashinfer_dcp_spec_reorder.py`

### Summary

**What changed and why**  
Moved the CUDA platform check before the import of flashinfer_backend in the test file. Previously, the import occurred before the check, causing a ModuleNotFoundError when flashinfer is not installed on non-CUDA platforms. The fix ensures the test is skipped before attempting the import.

**Technical impact**  
Fixes a buildkite test failure on non-CUDA platforms (e.g., ROCm) where flashinfer is not available. The test now correctly skips instead of crashing with an import error.

**Potential risks**  
Low risk. The change only reorders existing code and adds a blank line. The logic is identical; only the execution order is corrected.

**Key insights**  
The original code had a subtle ordering bug: the platform check was placed after the import, making it ineffective. This highlights the importance of placing guards before any platform-specific imports.

---

## 22. [[CI] Build macOS arm64 CPU wheel natively on the macmini queue](https://github.com/vllm-project/vllm/pull/48289)


### Base Information

- **PR Number:** #48289
- **Author:** [mgoin](https://github.com/mgoin)
- **Merged By:** [khluu](https://github.com/khluu)
- **Merged time:** 2026-07-14 12:40:27
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48289/files) (4):**
  - `.buildkite/release-pipeline.yaml`
  - `.buildkite/scripts/build-macos-wheel.sh`
  - `.buildkite/scripts/upload-nightly-wheels.sh`
  - `csrc/cpu/torch_bindings.cpp`

### Summary

**What changed and why**  
Added a new Buildkite pipeline step to build macOS arm64 CPU wheels natively on a dedicated macmini agent, replacing a previous GitHub Actions workflow. The upload script was modified to skip Linux-only auditwheel retagging for macOS wheels. A small C++ preprocessor guard was updated to exclude Apple ARM from a fused MoE kernel that requires BF16 support not available on macOS.

**Technical impact**  
Enables native macOS arm64 wheel builds within the CI pipeline, reducing reliance on external runners and simplifying the release process. The upload script now conditionally sources manylinux.sh and applies manylinux tags only for Linux, preventing errors on macOS. The C++ change prevents compilation failures on Apple Silicon by excluding the fused MoE kernel when building for macOS.

**Potential risks**  
The new build step depends on the macmini agent being properly provisioned with required tools (uv, Xcode CLT, protobuf, aws CLI). If the agent is not ready, the pipeline may fail. The C++ change may affect performance or functionality on macOS if the fused MoE kernel was previously compiled and used; however, it was likely never functional on macOS due to missing BF16 support.

**Key insights**  
The PR consolidates macOS wheel building into the main CI pipeline, removing a separate GHA workflow and associated tokens. The changes are well-structured with clear separation of build and upload steps, and include validation of wheel integrity via SHA256 checksums. The C++ guard update is a minimal but necessary fix to ensure cross-platform compatibility.

---

## 23. [[Reasoning] Optimize TPOT for thinking budget when used with speculative decoding](https://github.com/vllm-project/vllm/pull/46662)


### Base Information

- **PR Number:** #46662
- **Author:** [rishitdholakia13](https://github.com/rishitdholakia13)
- **Merged By:** [njhill](https://github.com/njhill)
- **Merged time:** 2026-07-14 11:55:41
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/46662/files) (3):**
  - `vllm/v1/sample/rejection_sampler.py`
  - `vllm/v1/sample/sampler.py`
  - `vllm/v1/sample/thinking_budget_state.py`

### Summary

**What changed and why**  
Removed redundant merging of output_token_ids with spec draft tokens when only thinking budget is active (no penalties/bad words). The thinking budget state holder now uses the original output_token_ids directly instead of the combined list, and the stripping of draft tokens from output_tok_ids in update_state is removed because it is no longer needed. This eliminates expensive Python list copies at long contexts.

**Technical impact**  
Per-step sampling overhead drops from ~38 ms to ~0.2 ms at long context (63000, bs=64) when thinking budget and speculative decoding are used together. Behavior of thinking budget remains unchanged.

**Potential risks**  
Low risk. The change is a performance optimization that removes unnecessary work. The thinking budget logic now operates on the correct token IDs (committed outputs only) as spec drafts are handled separately via spec_token_ids. The removed code in update_state was a workaround that is no longer needed.

**Key insights**  
The optimization avoids O(n) list operations per step by not combining output_token_ids with spec tokens unless penalties or bad words are present. The thinking budget state holder only needs the committed output tokens, not the combined list, so the merge was redundant.

---

## 24. [[ROCm] Retune MI355 selective_state_update float32 config on the unified effective_batch grid](https://github.com/vllm-project/vllm/pull/48373)


### Base Information

- **PR Number:** #48373
- **Author:** [vanshbhatia-amd](https://github.com/vanshbhatia-amd)
- **Merged By:** [tomeras91](https://github.com/tomeras91)
- **Merged time:** 2026-07-14 11:26:35
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48373/files) (1):**
  - `vllm/model_executor/layers/mamba/ops/configs/selective_state_update/headdim=64,dstate=128,device_name=AMD_Instinct_MI355_OAM,cache_dtype=float32.json`

### Summary

**What changed and why**  
Retuned the selective_state_update kernel configuration for AMD MI355X with cache_dtype=float32 to use the unified effective_batch grid (128 to 262144) instead of the previous narrower grid (10 to 5120). This aligns MI355X with MI300X and MI350X configs for consistent benchmarking and tuning across AMD Instinct GPUs.

**Technical impact**  
The config file was replaced with new BLOCK_SIZE_M and num_warps values for each effective_batch size, resulting in 1.1x to 1.6x speedup over the heuristic baseline. The change only affects Triton launch geometry, not kernel math, so model outputs remain unchanged (verified via lm_eval).

**Potential risks**  
Low risk. The config is purely a tuning parameter change with no code modifications. The new grid points may not be optimal for all deployment scenarios, but the uniform grid ensures consistent behavior across the AMD Instinct line. End-to-end correctness was validated with lm_eval, showing negligible accuracy differences within statistical noise.

**Key insights**  
The retuning provides significant performance improvements (up to 1.6x) for larger effective batch sizes, which are common in decode-heavy workloads. The float32 variant shows smaller gains than float16 due to memory-bound nature from doubled state traffic. The unified grid simplifies maintenance and comparison across AMD GPUs.

---

## 25. [[KV Offload] Split cpu_cache_usage_perc into write/read usage gauges](https://github.com/vllm-project/vllm/pull/47666)


### Base Information

- **PR Number:** #47666
- **Author:** [Srinivasoo7](https://github.com/Srinivasoo7)
- **Merged By:** [orozery](https://github.com/orozery)
- **Merged time:** 2026-07-14 10:13:42
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/47666/files) (4):**
  - `tests/v1/kv_offload/cpu/test_manager.py`
  - `vllm/v1/kv_offload/cpu/common.py`
  - `vllm/v1/kv_offload/cpu/manager.py`
  - `vllm/v1/kv_offload/cpu/spec.py`

### Summary

**What changed and why**  
Split the existing `cpu_cache_usage_perc` gauge into two new gauges (`cpu_cache_write_usage_perc` and `cpu_cache_read_usage_perc`) to distinguish between CPU cache space pinned by in-flight stores (GPU-to-CPU) and loads (CPU-to-GPU). The original gauge remains as the sum of both. This improves observability of offloading pressure.

**Technical impact**  
Adds a `_num_write_pending_blocks` counter to `CPUOffloadingManager`, incremented/decremented in `prepare_store`/`complete_store`. Read usage is derived as `usage - write_usage`. Two new metrics are registered and exposed via `get_stats()`. Tests verify correct split values under various scenarios including concurrent operations and failed stores.

**Potential risks**  
Low risk. The change is additive and does not alter existing behavior. The derived read usage assumes no overlap between write-pending and read-pending states, which holds by design. Edge cases like division by zero are handled. The new counter must be kept in sync with block state transitions; the implementation appears correct.

**Key insights**  
The split provides actionable insight into whether CPU cache pressure is due to writes or reads, enabling better tuning of offloading thresholds. The design avoids a separate counter for reads by leveraging the invariant that a block is either write-pending or read-pending, simplifying the implementation.

---

## 26. [[Doc] Sync four function docstrings with their signatures](https://github.com/vllm-project/vllm/pull/45437)


### Base Information

- **PR Number:** #45437
- **Author:** [DaoyuanLi2816](https://github.com/DaoyuanLi2816)
- **Merged By:** [yewentao256](https://github.com/yewentao256)
- **Merged time:** 2026-07-14 10:10:13
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/45437/files) (3):**
  - `vllm/_aiter_ops.py`
  - `vllm/_custom_ops.py`
  - `vllm/model_executor/layers/quantization/utils/fp8_utils.py`

### Summary

**What changed and why**  
Four function docstrings were corrected to match their actual signatures: fixed a typo (output_dytpe → output_dtype), replaced a stale parameter description (use_8x4_sf_layout) with the current parameters (is_sf_swizzled_layout, backend), added missing parameters (nLane, gate_up) and removed a non-existent parameter (n_splits). This ensures the rendered API documentation accurately reflects the code.

**Technical impact**  
No behavior change; only documentation strings are updated. The changes improve developer and user experience by providing correct parameter descriptions.

**Potential risks**  
None. The changes are purely cosmetic and do not affect any logic, tests, or runtime behavior.

**Key insights**  
The PR demonstrates a careful audit of docstrings against actual function signatures, catching both typos and stale documentation. It's a good example of maintaining documentation quality alongside code changes.

---

## 27. [[Bugfix] Make MLA+SWA check the layer's backend, not the model config](https://github.com/vllm-project/vllm/pull/48520)


### Base Information

- **PR Number:** #48520
- **Author:** [mgoin](https://github.com/mgoin)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-07-14 09:53:34
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48520/files) (1):**
  - `vllm/model_executor/layers/attention/attention.py`

### Summary

**What changed and why**  
Changed the assertion in `get_kv_cache_spec` from checking `vllm_config.model_config.use_mla` to checking `self.attn_backend.is_mla()`. The old code incorrectly rejected sliding-window layers in a drafter when the target model uses MLA, because it checked the model-level config instead of the specific layer's backend. The fix ensures the invariant that an MLA layer must not use sliding window is enforced per-layer, avoiding false positives.

**Technical impact**  
Fixes a bug where a drafter with a sliding-window layer would fail assertion when the target model is MLA. The change is minimal (2 lines) and only affects the assertion condition, so the behavior is corrected without altering other logic. The sliding-window block size logic remains unchanged.

**Potential risks**  
Low risk. The change is a straightforward correction of the condition. However, if any code path relies on the old behavior (e.g., expecting the model-level check to catch some other scenario), it could be affected. But the old behavior was incorrect, so such reliance would be a bug itself.

**Key insights**  
The bug highlights the importance of checking per-layer attributes rather than model-level configs when enforcing constraints that vary across layers. The fix uses `self.attn_backend.is_mla()` which correctly reflects the actual attention backend of the layer, making the assertion accurate for both target models and drafters.

---

## 28. [[CI Bug] Fully solve accuracy issue for DSv3.2 + MTP + Sequence Parallel](https://github.com/vllm-project/vllm/pull/48036)


### Base Information

- **PR Number:** #48036
- **Author:** [yewentao256](https://github.com/yewentao256)
- **Merged By:** [yewentao256](https://github.com/yewentao256)
- **Merged time:** 2026-07-14 07:00:24
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48036/files) (3):**
  - `vllm/config/parallel.py`
  - `vllm/model_executor/models/deepseek_mtp.py`
  - `vllm/models/deepseek_v32/nvidia/mtp.py`

### Summary

**What changed and why**  
Removed the data_parallel_size > 1 condition from use_sequence_parallel_moe to allow sequence parallel MoE even when data parallel size is 1, fixing an accuracy regression for DeepSeek V3.2 with MTP and sequence parallel. Added _restore_full_token_layout_if_needed helper to gather and slice hidden states after SP MoE layers in both deepseek_mtp.py and the NVIDIA-specific mtp.py, ensuring correct token layout for MTP proposer.

**Technical impact**  
Enables sequence parallel MoE for single-data-parallel configurations, which was previously blocked. The new helper ensures that after SP MoE layers, the hidden states are properly gathered across tensor-parallel ranks and trimmed to the original number of tokens, preserving correctness for the MTP draft model. This fixes accuracy degradation observed in GSM8K evaluations.

**Potential risks**  
The change modifies a core parallel configuration property, which could affect other models using sequence parallel MoE. The all-gather operation adds communication overhead, but it is only applied when the token count differs (i.e., when SP is active). The helper assumes the combined hidden_states and residual are contiguous; if not, the split may produce incorrect results. Testing is limited to DeepSeek V3.2 with MTP; other models may need similar adjustments.

**Key insights**  
The bug was caused by the data_parallel_size > 1 guard preventing SP MoE from being used in single-data-parallel scenarios, which led to incorrect token layout for MTP. The fix restores the full token layout after SP MoE layers by gathering across tensor-parallel ranks and slicing to the original token count. This is a targeted fix for a specific model configuration, but the pattern may be applicable to other models using SP with MTP.

---

## 29. [[Bugfix] Gemma4 parser: classify channel-less output consistently in streaming and non-streaming](https://github.com/vllm-project/vllm/pull/48262)


### Base Information

- **PR Number:** #48262
- **Author:** [adhi29](https://github.com/adhi29)
- **Merged By:** [bbrowning](https://github.com/bbrowning)
- **Merged time:** 2026-07-14 06:30:16
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48262/files) (2):**
  - `tests/parser/engine/test_gemma4_streaming_reasoning.py`
  - `vllm/parser/gemma4.py`

### Summary

**What changed and why**  
Fixed a bug where the Gemma4 streaming parser pre-initialized the engine to REASONING for any new model turn, causing channel-less output to be misclassified as reasoning in streaming mode while non-streaming correctly classified it as content. The fix narrows pre-initialization to only when the prompt ends inside an open <\\|channel> block (post-tool-response continuation), and starts new turns in CONTENT, relying on the existing (CONTENT, THINK_START) transition for model-opened channels.

**Technical impact**  
Ensures consistent classification of channel-less output between streaming and non-streaming paths. The new method _prompt_ends_in_open_reasoning scans prompt tokens backwards to detect open reasoning blocks, replacing the previous is_reasoning_end check that was too broad. This changes the initial state logic for streaming, affecting all Gemma4 streaming requests with thinking enabled.

**Potential risks**  
The change is scoped to the initial state adjustment and is covered by existing tests plus new tests. The is_reasoning_end method is intentionally unchanged to preserve structured output grammar gating. Risk is low as the fix aligns streaming behavior with non-streaming, and the open-channel continuation case from #45834 is still handled correctly.

**Key insights**  
The root cause was conflating two distinct scenarios: a prompt ending inside an open reasoning block (where pre-initialization to REASONING is correct) versus a new model turn (where the model may or may not emit reasoning markers). The fix uses a targeted backward scan to distinguish these cases, avoiding the need for broader architectural changes.

---

## 30. [[Bugfix] Include inline per-token-head scales in offloaded page transfer width](https://github.com/vllm-project/vllm/pull/48411)


### Base Information

- **PR Number:** #48411
- **Author:** [Etelis](https://github.com/Etelis)
- **Merged By:** [orozery](https://github.com/orozery)
- **Merged time:** 2026-07-14 06:07:27
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48411/files) (3):**
  - `tests/v1/core/test_kv_cache_utils.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/offloading/worker.py`
  - `vllm/v1/kv_cache_interface.py`

### Summary

**What changed and why**  
Bug fix: offloaded KV pages with per-token-head quantization were truncated because the transfer width used `real_page_size_bytes`, which excludes inline fp32 scales. A new property `unpadded_page_size_bytes` is introduced that includes these scales, and the offloading connector now uses it. The `page_size_bytes` property is refactored to delegate to `unpadded_page_size_bytes`. Three unit tests verify the behavior.

**Technical impact**  
Fixes data corruption during KV cache offloading for per-token-head quantized models. The change is minimal (3 files, +41/-6) and only affects the offloading path. The new property `unpadded_page_size_bytes` is added to `AttentionSpec`, and the offloading worker uses it instead of `real_page_size_bytes`. The `page_size_bytes` property now correctly accounts for inline scales when no padding is specified.

**Potential risks**  
Low risk. The change is localized to the offloading connector and the page size computation. The new property is only used in the offloading worker. The existing `page_size_bytes` behavior is preserved (delegates to `unpadded_page_size_bytes`). The note about single-group models having a separate issue is acknowledged but not addressed here.

**Key insights**  
The root cause was a mismatch between the memory layout (inline scales) and the transfer width. The fix separates the concept of 'unpadded page size' (including scales) from 'real page size' (raw KV data). The test plan covers unquantized, per-token-head quantized, and padded cases, ensuring correctness.

---

## 31. [[NIXL] Avoid reading expired blocks in bidirectional turn-2 read](https://github.com/vllm-project/vllm/pull/47021)


### Base Information

- **PR Number:** #47021
- **Author:** [tomerg-nvidia](https://github.com/tomerg-nvidia)
- **Merged By:** [NickLucche](https://github.com/NickLucche)
- **Merged time:** 2026-07-14 06:03:41
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/47021/files) (10):**
  - `docs/design/nixl_kv_cache_lease.md`
  - `tests/v1/kv_connector/unit/test_bidirectional_kv_transfer.py`
  - `tests/v1/kv_connector/unit/test_nixl_connector.py`
  - `tests/v1/kv_connector/unit/test_nixl_connector_hma.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/nixl/base_scheduler.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/nixl/base_worker.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/nixl/metadata.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/nixl/pull_scheduler.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/nixl/pull_worker.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/nixl/push_worker.py`

### Summary

**What changed and why**  
Added clock-sync handshake and expiry-time check to prevent reading expired KV blocks in bidirectional turn-2 transfers. D now sends its perf_counter in the handshake reply, P estimates clock offset, and D includes block expiry time in kv_transfer_params. P declines reads when blocks are near or past expiry.

**Technical impact**  
Bumps NIXL_CONNECTOR_VERSION from 4 to 5. Handshake now returns a tuple (agents, offset) and uses recv_multipart to receive an extra timestamp frame. Pull worker gains _is_turn2_read_expired gate that uses clock offset and safety margin. Scheduler exports remote_blocks_expiry_time for D requests. Tests updated to match new handshake signature and verify expiry logic.

**Potential risks**  
Clock offset estimation assumes symmetric network latency; asymmetric RTT could cause false positives/negatives. Safety margin (5s) may need tuning for high-latency networks. Handshake change breaks compatibility with older peers (version bump). The expiry gate only applies when bidirectional_kv_xfer is enabled; misconfiguration could skip the check.

**Key insights**  
The fix addresses a real data corruption bug where expired blocks were reused. Clock offset estimation via handshake RTT is a pragmatic approach without NTP. The safety margin and per-engine offset cache provide robustness. Tests cover expired, near-expiry, offset-adjusted, and missing-deadline cases.

---

## 32. [[Model] Enable LoRA support for tower and connector in LlavaNextVideo](https://github.com/vllm-project/vllm/pull/48594)


### Base Information

- **PR Number:** #48594
- **Author:** [gangula-karthik](https://github.com/gangula-karthik)
- **Merged By:** [noooop](https://github.com/noooop)
- **Merged time:** 2026-07-14 05:09:39
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48594/files) (3):**
  - `docs/models/supported_models.md`
  - `tests/models/multimodal/processing/test_llava_next_video.py`
  - `vllm/model_executor/models/llava_next_video.py`

### Summary

**What changed and why**  
Added LoRA support for the vision tower and connector in LlavaNextVideoForConditionalGeneration by implementing SupportsLoRA, packed_modules_mapping, get_mm_mapping(), get_num_mm_encoder_tokens(), and get_num_mm_connector_tokens(). Also added a unit test for the new token count methods and updated the supported models documentation to mark LoRA as supported.

**Technical impact**  
Enables LoRA fine-tuning of the vision tower and multi-modal projector for LlavaNextVideo, which was previously unsupported. The new helper methods correctly handle spatial pooling differences between encoder and connector token counts, ensuring accurate LoRA module targeting.

**Potential risks**  
The token count methods assume integer division without remainder; if num_video_tokens is not a multiple of pooled_tokens_per_frame, the result may be incorrect. However, this is consistent with the model's design where placeholder counts are always multiples. The changes are well-tested and follow established patterns from other models.

**Key insights**  
The PR carefully handles the non-trivial token count inversion due to spatial pooling, using patch_grid_length and pooled_grid_length derived from the config. The test suite covers round-trip correctness for various frame counts and edge cases, and manual GPU verification confirmed real tensor shapes match computed counts.

---

## 33. [[Bugfix][Security] Fix concurrent sparse invariant race bypassing CVE remediation](https://github.com/vllm-project/vllm/pull/48583)


### Base Information

- **PR Number:** #48583
- **Author:** [jperezdealgaba](https://github.com/jperezdealgaba)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-07-14 04:08:25
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48583/files) (5):**
  - `tests/renderers/test_sparse_tensor_concurrent_race.py`
  - `vllm/multimodal/media/audio.py`
  - `vllm/multimodal/media/image.py`
  - `vllm/renderers/embed_utils.py`
  - `vllm/utils/sparse_utils.py`

### Summary

**What changed and why**  
Added a thread-safe wrapper `check_sparse_tensor_invariants_threadsafe()` that serializes access to PyTorch's process-global sparse invariant flag using a `threading.Lock`. Replaced all direct uses of `torch.sparse.check_sparse_tensor_invariants()` in audio, image, and embed_utils modules with the new wrapper. Added comprehensive tests to verify the fix prevents concurrent race conditions that could bypass CVE remediation.

**Technical impact**  
Fixes a security vulnerability where concurrent prompt-embedding loads could race the save/restore of the global sparse invariant flag, allowing malicious sparse tensors to bypass integrity checks. The lock ensures mutual exclusion, preventing the race condition. The change is minimal and localized to the sparse invariant context manager usage.

**Potential risks**  
The lock introduces a serialization bottleneck for concurrent sparse tensor loads, potentially reducing throughput under high concurrency. However, this is acceptable given the security fix. The lock is a `threading.Lock`, which is reentrant and safe for use with context managers. No deadlock risk is introduced as the lock is not held across other locks. The tests cover concurrent valid and invalid loads to ensure correctness.

**Key insights**  
The race condition arises because PyTorch's `check_sparse_tensor_invariants()` modifies a process-global flag without thread safety. The fix uses a simple lock to serialize access, which is a standard pattern for protecting global state. The tests include a forced interleaving test that reproduces the exact race condition, demonstrating the vulnerability and its fix.

---

## 34. [[Misc] Rename VLLM_TRITON_ATTN_USE_TD to VLLM_TRITON_USE_TD](https://github.com/vllm-project/vllm/pull/45781)


### Base Information

- **PR Number:** #45781
- **Author:** [afierka-intel](https://github.com/afierka-intel)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-07-14 03:32:58
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/45781/files) (3):**
  - `vllm/envs.py`
  - `vllm/v1/attention/backends/triton_attn.py`
  - `vllm/v1/attention/ops/triton_unified_attention.py`

### Summary

**What changed and why**  
Renamed environment variable VLLM_TRITON_ATTN_USE_TD to VLLM_TRITON_USE_TD to generalize the tensor-descriptor (TD) feature beyond attention kernels, as per RFC #42545. The old name is deprecated with a warning and will be removed in v0.25. Updated all references in envs.py, triton_attn.py, and triton_unified_attention.py.

**Technical impact**  
Changes the environment variable name used to control TD behavior. Code now reads VLLM_TRITON_USE_TD instead of VLLM_TRITON_ATTN_USE_TD. The old variable is still recognized but ignored with a deprecation warning. The tri-state semantics (unset/1/0) remain unchanged. This is a non-breaking change for users who update their env vars, but those using the old name will see a warning.

**Potential risks**  
Users relying on the old variable name will get a deprecation warning but no functional change. If they ignore the warning, their configuration will be silently ignored after v0.25. There is a small risk of confusion if users set both variables with conflicting values; the new variable takes precedence. The change is backward-compatible for now.

**Key insights**  
The rename aligns with a broader strategy to use a single TD flag across all Triton kernels. The deprecation pattern mirrors existing practices (e.g., HOST_IP to VLLM_HOST_IP). The inline warning approach was chosen because the generic deprecated_env helper was removed. The comment update from 'Intel Xe2/Xe3' to 'Intel XPU' reflects a more accurate platform description.

---

## 35. [[Test] Enable KV cache events for HMA models in CPU offloading test](https://github.com/vllm-project/vllm/pull/47754)


### Base Information

- **PR Number:** #47754
- **Author:** [Etelis](https://github.com/Etelis)
- **Merged By:** [orozery](https://github.com/orozery)
- **Merged time:** 2026-07-14 02:22:27
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/47754/files) (1):**
  - `tests/v1/kv_connector/unit/test_offloading_connector.py`

### Summary

**What changed and why**  
Removed the conditional guard that prevented KV cache events from being enabled for HMA models in the CPU offloading test. Previously, KV events were incompatible with HMA, but recent changes (#37688 and #43468) removed that restriction, so the guard is no longer needed.

**Technical impact**  
The test now enables KV cache events for all models, including HMA models, increasing test coverage. The test logic is simplified by removing the conditional branch, making the code more uniform.

**Potential risks**  
Low risk. The change only affects test code and relies on upstream changes that have already been merged. If the upstream changes are incomplete, the test might fail for HMA models, but that would indicate a real issue.

**Key insights**  
The removal of the `uses_hma` guard reflects the evolution of the KV cache event system to support hybrid KV cache managers. This change ensures that the test exercises the event system for all model types, improving regression detection.

---

## 36. [[XPU][CI] Add tests/v1/e2e/general/test_correctness_sliding_window.py in Intel GPU CI](https://github.com/vllm-project/vllm/pull/47231)


### Base Information

- **PR Number:** #47231
- **Author:** [zxd1997066](https://github.com/zxd1997066)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-07-14 01:50:17
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/47231/files) (2):**
  - `.buildkite/intel_jobs/test-intel.yaml`
  - `.buildkite/scripts/hardware_ci/run-intel-ci-test.sh`

### Summary

**What changed and why**  
Added a new test file `tests/v1/e2e/general/test_correctness_sliding_window.py` to the Intel GPU CI pipeline, with one specific test deselected. This expands CI coverage for sliding window correctness on XPU hardware.

**Technical impact**  
The change adds a new pytest command to two CI configuration files, enabling execution of sliding window correctness tests on Intel GPU. One test case is deselected, likely due to known issues or incompatibility with the hardware.

**Potential risks**  
The new test may increase CI runtime. The deselected test might indicate a known failure that could be a symptom of a deeper issue. If the test suite is not properly isolated, it could affect other tests.

**Key insights**  
The PR focuses on improving test coverage for sliding window functionality on Intel GPUs, which is important for ensuring model correctness. The deselection of a specific test suggests a targeted approach to avoid known failures while still adding coverage.

---

## 37. [[Bugfix] Return 400 instead of 500 when multimodal data is sent to a text-only model](https://github.com/vllm-project/vllm/pull/48473)


### Base Information

- **PR Number:** #48473
- **Author:** [hnt2601](https://github.com/hnt2601)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-07-14 01:15:44
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48473/files) (2):**
  - `tests/renderers/test_process_multi_modal_uuids.py`
  - `vllm/renderers/base.py`

### Summary

**What changed and why**  
Moved the line that accesses `self._mm_req_counter` after the call to `self.get_mm_processor()` in `_process_multimodal`. Previously, the counter was accessed before the processor was resolved, causing an `AttributeError` on text-only models (since the counter is only initialized for multimodal models). This change makes the existing `ValueError` guard in `get_mm_processor()` reachable, so the error correctly maps to HTTP 400 instead of 500.

**Technical impact**  
Fixes a bug where sending multimodal data to a text-only model resulted in an HTTP 500 Internal Server Error. Now it returns HTTP 400 Bad Request, which is the correct client-error status. The change is minimal (2 lines moved) and does not affect any other code paths.

**Potential risks**  
Low risk. The change only reorders two lines, and the moved line (`mm_req_id = ...`) is now executed after the processor is guaranteed to exist. For multimodal models, behavior is identical because `get_mm_processor()` succeeds and the counter is available. For text-only models, the `ValueError` is now raised before the counter access, which is the intended behavior.

**Key insights**  
The bug was caused by an ordering issue: the `_mm_req_counter` attribute was accessed before the processor was resolved, making the existing guard in `get_mm_processor()` dead code. The fix is a simple reorder that makes the guard effective. Two new tests verify the fix and ensure the error maps to HTTP 400.

---

## 38. [Add Cosmos3 Edge Reasoner model](https://github.com/vllm-project/vllm/pull/48291)


### Base Information

- **PR Number:** #48291
- **Author:** [adsridhar](https://github.com/adsridhar)
- **Merged By:** [Isotr0py](https://github.com/Isotr0py)
- **Merged time:** 2026-07-14 01:14:51
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48291/files) (13):**
  - `docs/models/supported_models.md`
  - `tests/models/multimodal/processing/test_cosmos3_edge.py`
  - `tests/models/multimodal/test_mapping.py`
  - `tests/models/registry.py`
  - `vllm/model_executor/models/cosmos3.py`
  - `vllm/model_executor/models/cosmos3_edge.py`
  - `vllm/model_executor/models/nemotron_h.py`
  - `vllm/model_executor/models/registry.py`
  - `vllm/transformers_utils/config.py`
  - `vllm/transformers_utils/configs/__init__.py`
  - `vllm/transformers_utils/configs/cosmos3_edge.py`
  - `vllm/transformers_utils/processors/__init__.py`
  - `vllm/transformers_utils/processors/cosmos3_edge.py`

### Summary

**What changed and why**  
Added a new Cosmos3 Edge Reasoner model (not yet released) to vllm. The model is a mixture of transformers with a Reasoner and Generator tower. This PR introduces custom model layers (Cosmos3EdgeVisionEncoder, Cosmos3EdgePatchMerger, Cosmos3EdgeVisionModel), a custom processor (Cosmos3EdgeProcessor), checkpoint weight mapping, and configuration classes. The existing Cosmos3 model was also refactored to unify weight loading patterns.

**Technical impact**  
Adds support for a new multimodal model architecture (Cosmos3 Edge) with custom vision encoder, projector, and text decoder layers. The model leverages existing Nemotron-H attention and MLP layers with interleaved multimodal RoPE. The processor extends Siglip2ImageProcessor and Qwen3VLVideoProcessor for image/video preprocessing. The change also modifies the NemotronHModel to accept custom decoder layer types, enabling reuse for Cosmos3 Edge. The registry and config files are updated to register the new model.

**Potential risks**  
The model is not yet released, so the implementation may need adjustments once the official checkpoint is available. The custom weight mapper and processor may have edge cases not covered by tests. The changes to NemotronHModel (decoder_layer_types parameter) could affect existing Nemotron-H models if not used correctly. The new processor's image resizing logic (round_by_factor, ceil_by_factor) may produce different results than the original Siglip2 processor.

**Key insights**  
The Cosmos3 Edge model reuses many components from existing models (Nemotron-H, Qwen3-VL, Siglip2), demonstrating vllm's modular design. The weight mapper handles the complex checkpoint structure where generation-only tensors are dropped. The processor uses a custom image resizing strategy that respects pixel count constraints while maintaining divisibility by patch and merge sizes. The PR also refactors Cosmos3 to use a unified allow_patterns_overrides for loading weights from both transformer/ and vision_encoder/ directories.

---

## 39. [fix(security): guard lm-format-enforcer regex compile with timeout](https://github.com/vllm-project/vllm/pull/47595)


### Base Information

- **PR Number:** #47595
- **Author:** [jperezdealgaba](https://github.com/jperezdealgaba)
- **Merged By:** [khluu](https://github.com/khluu)
- **Merged time:** 2026-07-14 00:18:12
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/47595/files) (1):**
  - `vllm/v1/structured_output/backend_lm_format_enforcer.py`

### Summary

**What changed and why**  
Added timeout protection to regex compilation in lm-format-enforcer backend by using compile_regex_with_timeout, and added early validation of regex patterns in validate_structured_output_request_lm_format_enforcer to match security fixes applied to xgrammar and outlines backends.

**Technical impact**  
Prevents regex denial-of-service attacks by enforcing a timeout on regex compilation, and catches invalid regex patterns early during request validation rather than during grammar compilation.

**Potential risks**  
Low risk; the change is consistent with existing patterns in other backends. The timeout may cause previously valid (but slow) regexes to fail, but this is the intended security improvement.

**Key insights**  
This completes the security hardening for all structured output backends, ensuring consistent protection against regex-based DoS attacks across the codebase.

---

