# vLLM Merged PR Report

**Report Date:** 2026-06-02 PDT

**Total Merged PRs:** 61

---

## 1. [[Perf] Add tuned selective_state_update configs for H200 and RTX PRO …](https://github.com/vllm-project/vllm/pull/44251)


### Base Information

- **PR Number:** #44251
- **Author:** [Majid-Taheri](https://github.com/Majid-Taheri)
- **Merged By:** [tomeras91](https://github.com/tomeras91)
- **Merged time:** 2026-06-02 23:59:01
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44251/files) (4):**
  - `vllm/model_executor/layers/mamba/ops/configs/selective_state_update/headdim=64,dstate=128,device_name=NVIDIA_H200,cache_dtype=float16.json`
  - `vllm/model_executor/layers/mamba/ops/configs/selective_state_update/headdim=64,dstate=128,device_name=NVIDIA_H200,cache_dtype=float32.json`
  - `vllm/model_executor/layers/mamba/ops/configs/selective_state_update/headdim=64,dstate=128,device_name=NVIDIA_RTX_PRO_6000_Blackwell_Server_Edition,cache_dtype=float16.json`
  - `vllm/model_executor/layers/mamba/ops/configs/selective_state_update/headdim=64,dstate=128,device_name=NVIDIA_RTX_PRO_6000_Blackwell_Server_Edition,cache_dtype=float32.json`

### Summary

**What changed and why**  
Added four tuned configuration JSON files for the selective_state_update kernel targeting NVIDIA H200 and RTX PRO 6000 Blackwell GPUs, covering both float16 and float32 cache dtypes. These configs were generated via benchmarking to replace the default Triton heuristic, which left significant performance on the table for these devices.

**Technical impact**  
Improves kernel-level performance by up to 2.69x on H200 and up to 1.49x on RTX PRO 6000 Blackwell for the selective_state_update operation. End-to-end throughput gains of ~2.6% were measured on a Nemotron-Nano-9B-v2 model with H200. The configs are automatically loaded by the existing config loader, requiring no code changes.

**Potential risks**  
Low risk. The configs are device-specific and validated against the heuristic with atol=0.01. If the Triton version or hardware behavior changes, the configs may become suboptimal, but the loader falls back to heuristic if no matching config is found. The PR only adds new files and does not modify any existing code.

**Key insights**  
This is a follow-up to PR #43083, extending coverage to H200 and RTX PRO 6000 Blackwell. The tuning process used a benchmark script to sweep BLOCK_SIZE_M and num_warps parameters. The configs are stored in a structured directory format keyed by headdim, dstate, device_name, and cache_dtype, matching the naming convention from the previous PR.

---

## 2. [[ROCm][CI] Optimize ROCm Docker build: registry cache, DeepEP, and ci-bake script](https://github.com/vllm-project/vllm/pull/36949)


### Base Information

- **PR Number:** #36949
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-06-02 23:43:08
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36949/files) (10):**
  - `.buildkite/ci_config_rocm.yaml`
  - `.buildkite/hardware_tests/amd.yaml`
  - `.buildkite/scripts/ci-bake-rocm.sh`
  - `.buildkite/scripts/hardware_ci/run-amd-test.sh`
  - `.dockerignore`
  - `cmake/utils.cmake`
  - `docker/Dockerfile.rocm`
  - `docker/ci-rocm.hcl`
  - `docker/docker-bake-rocm.hcl`
  - `tools/install_torchcodec_rocm.sh`

### Summary

**What changed and why**  
This PR introduces a three-tier Docker build system for ROCm CI to reduce per-PR build times from ~26 minutes to a few minutes. It adds a pre-built ci_base image (built weekly) containing stable layers like RIXL, DeepEP, rocshmem, and torchcodec, so per-PR builds only rebuild the thin vLLM wheel and workspace. New files include ci_config_rocm.yaml, ci-bake-rocm.sh, ci-rocm.hcl, and docker-bake-rocm.hcl. Existing files like Dockerfile.rocm, amd.yaml, run-amd-test.sh, .dockerignore, cmake/utils.cmake, and install_torchcodec_rocm.sh are modified to support the new caching and build orchestration.

**Technical impact**  
Reduces per-PR Docker build time from ~26 minutes to under 5 minutes by caching stable layers. Introduces content-addressed caching for ci_base and dependency stages, using Docker Hub registry cache. Adds a new ci-bake-rocm.sh script that orchestrates builds with bake, computes content hashes, and manages cache keys. Modifies the Dockerfile to split native extension compilation into a separate csrc-build stage, enabling reuse of compiled ROCm objects. Adds ccache and mold linker to speed up compilation. Changes CI pipeline to first ensure ci_base is up-to-date, then build the test image. Adds artifact-based image assembly for test jobs to avoid pulling large images.

**Potential risks**  
The new caching logic is complex and may have edge cases where cache invalidation fails, leading to stale images. The ci-bake-rocm.sh script is large (1749 lines) and could contain bugs. The dependency on Docker Hub for cache may introduce latency or rate limits. The split of native build into csrc-build stage may cause issues if the precompiled wheel is not correctly reused. The artifact-based image assembly adds complexity to test job setup. The changes to cmake/utils.cmake to chain hipify targets could affect build parallelism. The new ci_config_rocm.yaml may trigger unnecessary rebuilds if patterns are too broad.

**Key insights**  
The PR significantly optimizes CI build times by leveraging Docker layer caching and a three-tier image strategy. The introduction of content-addressed caching for dependency stages (RIXL, DeepEP, etc.) ensures that upstream changes are automatically detected and rebuilt. The use of ccache and mold linker accelerates compilation. The artifact-based test image assembly reduces network transfer and Docker Hub pull times. The PR is tightly coupled with ci-infra PR #307 and requires coordinated merge. The changes are well-documented in the PR description and code comments.

---

## 3. [Enable perf_token_group_quant/_C_stable_libtorch for ROCm](https://github.com/vllm-project/vllm/pull/42758)


### Base Information

- **PR Number:** #42758
- **Author:** [charlifu](https://github.com/charlifu)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-06-02 23:23:28
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/42758/files) (12):**
  - `CMakeLists.txt`
  - `cmake/hipify.py`
  - `csrc/libtorch_stable/ops.h`
  - `csrc/libtorch_stable/quantization/w8a8/fp8/per_token_group_quant.cu`
  - `csrc/libtorch_stable/torch_bindings.cpp`
  - `csrc/libtorch_stable/torch_utils.h`
  - `tests/kernels/quantization/test_per_token_group_quant.py`
  - `vllm/compilation/passes/fusion/matcher_utils.py`
  - `vllm/compilation/passes/fusion/rms_quant_fusion.py`
  - `vllm/model_executor/layers/quantization/input_quant_fp8.py`
  - `vllm/model_executor/layers/quantization/utils/fp8_utils.py`
  - `vllm/model_executor/layers/quantization/utils/int8_utils.py`

### Summary

**What changed and why**  
This PR enables the `per_token_group_quant` kernels (FP8 and INT8) for ROCm by modifying the CUDA kernels to support warp sizes 64 and 32, updating the `GroupReduceMax` device function for 64-lane wavefronts, fixing the `hipify.py` script to correctly handle CMake builds, and moving the kernel source files from CUDA-only to the general compilation target `_C_stable_libtorch`. Additionally, it updates Python compilation passes and test files to remove CUDA-only guards and use platform-agnostic checks.

**Technical impact**  
The change extends ROCm support for per-token-group quantization, which is critical for FP8 and INT8 inference on AMD GPUs. It modifies the build system (CMakeLists.txt) to include the kernel sources for both CUDA and HIP, updates the hipify script to handle include paths and file renaming correctly, and adjusts the kernel code to work with ROCm's warp size. The Python-side changes remove `is_cuda()` guards and replace them with `is_cuda_alike()` to enable the same code paths on ROCm. This may affect performance and correctness on ROCm, but the changes are designed to maintain compatibility with CUDA.

**Potential risks**  
The changes to the `GroupReduceMax` function and shuffle masks may introduce subtle bugs on CUDA if the new ROCm-specific code paths are not properly guarded or if the mask calculations are incorrect. The hipify script modifications could break existing CUDA-only builds if the new include root or file copying logic is not robust. The removal of the `forward_hip` fallback in `input_quant_fp8.py` might cause regressions on ROCm if the native kernel path is not fully functional. Testing on both CUDA and ROCm is essential to validate correctness.

**Key insights**  
The PR demonstrates a systematic approach to enabling ROCm support by modifying kernel code, build scripts, and Python infrastructure. The use of `is_cuda_alike()` instead of `is_cuda()` is a good practice for platform-agnostic code. The changes to `hipify.py` address a known issue with header resolution and file renaming, which is crucial for HIP builds. The PR also refactors the quantization ops registration to be platform-agnostic, which simplifies future maintenance.

---

## 4. [[Benchmark] Enable reasoning-model (thinking) benchmarking via `--chat-template-kwargs` for client-rendered datasets](https://github.com/vllm-project/vllm/pull/44244)


### Base Information

- **PR Number:** #44244
- **Author:** [qiching](https://github.com/qiching)
- **Merged By:** [benchislett](https://github.com/benchislett)
- **Merged time:** 2026-06-02 22:49:51
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44244/files) (3):**
  - `tests/benchmarks/test_custom_dataset_chat_template_kwargs.py`
  - `vllm/benchmarks/datasets/datasets.py`
  - `vllm/benchmarks/serve.py`

### Summary

**What changed and why**  
Added --chat-template-kwargs CLI option to vllm bench serve, forwarded to client-side apply_chat_template for custom and speed_bench datasets, enabling reasoning-model thinking mode benchmarking. Previously, these datasets rendered prompts without chat_template_kwargs, preventing reasoning mode activation.

**Technical impact**  
Enables acceptance-length and other metrics to be measured under realistic reasoning workloads for speculative decoding benchmarks. The change is backward-compatible (default None preserves existing behavior).

**Potential risks**  
Low risk; the new parameter is optional and defaults to None. Incorrect JSON input could cause errors, but argparse handles parsing. The change only affects client-side rendering path, not server-side chat completions.

**Key insights**  
The fix addresses a gap where --extra-body and --default-chat-template-kwargs could not influence client-side rendering. The unit test verifies kwargs propagation and default behavior, ensuring no regression.

---

## 5. [[Bugfix] fix crash in postprocess for null tool args](https://github.com/vllm-project/vllm/pull/43862)


### Base Information

- **PR Number:** #43862
- **Author:** [william-rom](https://github.com/william-rom)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-06-02 22:17:27
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/43862/files) (2):**
  - `tests/entrypoints/test_chat_utils.py`
  - `vllm/entrypoints/chat_utils.py`

### Summary

**What changed and why**  
Fixed a crash in _postprocess_messages when tool call arguments are the JSON string 'null'. json.loads('null') returns Python None, which later causes Jinja2 templates to fail. Added a None check so that parsed None is coerced to an empty dict.

**Technical impact**  
Prevents a crash in chat template rendering when tool calls have arguments='null'. The fix ensures the function's documented behavior (arguments=None or empty string becomes {}) is actually enforced for the JSON null case.

**Potential risks**  
Low risk. The change only adds a None check after json.loads, which is a safe operation. Existing behavior for valid JSON objects/arrays and empty strings is unchanged.

**Key insights**  
The bug was caused by a mismatch between the comment and the code: json.loads('null') returns None, which was not handled. The fix is minimal and targeted, with a regression test covering the exact edge case.

---

## 6. [fix: resolve CUTLASS fmin compatibility for DeepSeek-V4 init](https://github.com/vllm-project/vllm/pull/44236)


### Base Information

- **PR Number:** #44236
- **Author:** [Oxygen56](https://github.com/Oxygen56)
- **Merged By:** [zyongye](https://github.com/zyongye)
- **Merged time:** 2026-06-02 22:07:10
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44236/files) (1):**
  - `vllm/models/deepseek_v4/nvidia/ops/sparse_attn_compress_cutedsl.py`

### Summary

**What changed and why**  
Replaced 4 occurrences of `cute.arch.fmin` with `cutlass.min` in `sparse_attn_compress_cutedsl.py` to fix an `AttributeError` during JIT compilation. The `fmin` function is missing from `cute.arch` in the default `nvidia-cutlass-dsl-libs-base` package for CUDA 12.x, while `cutlass.min` is available in both `libs-base` and `libs-cu13` and is already used elsewhere in vLLM.

**Technical impact**  
Fixes a crash on CUDA 12.x builds when the sparse-attention compressor kernel is JIT-compiled, enabling DeepSeek-V4 initialization to succeed. The change is functionally equivalent for Float32 operands, lowering to the same NVVM `fmin` instruction.

**Potential risks**  
Low risk. `cutlass.min` is the canonical API and is already used in other parts of the codebase. The change is a direct replacement with no behavioral difference for the intended use case. However, if there are edge cases where `cutlass.min` behaves differently (e.g., for non-Float32 types), those could be affected, but the code only uses Float32.

**Key insights**  
The root cause is a missing `fmin` export in the base CUTLASS DSL package for CUDA 12.x, which vLLM intentionally installs without the `[cu13]` extra. This fix uses the more portable `cutlass.min` API, which is part of the core CuTe DSL and avoids dependency on the extra package. The issue was reported in multiple tickets and affects all CUDA 12.x users of DeepSeek-V4.

---

## 7. [Nit Changes in Tiered KV Offload](https://github.com/vllm-project/vllm/pull/44293)


### Base Information

- **PR Number:** #44293
- **Author:** [rshavitt](https://github.com/rshavitt)
- **Merged By:** [njhill](https://github.com/njhill)
- **Merged time:** 2026-06-02 21:53:22
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44293/files) (3):**
  - `tests/v1/kv_offload/tiering/test_fs_tier.py`
  - `tests/v1/kv_offload/tiering/test_tiering_offloading.py`
  - `vllm/v1/kv_offload/tiering/fs/manager.py`

### Summary

**What changed and why**  
Added documentation to the FileSystemTierManager class docstring explaining how to enable cross-process sharing of KV cache by setting PYTHONHASHSEED environment variable. No code logic changed.

**Technical impact**  
No impact on runtime behavior; only improves developer documentation for configuring cross-process KV cache sharing.

**Potential risks**  
None; documentation-only change with no code modifications.

**Key insights**  
The change clarifies a subtle dependency on Python's hash seed for consistent block filenames across processes, which is critical for shared filesystem deployments.

---

## 8. [[CI] Add missing vllm/parser/ CI trigger and fix test_parse.py](https://github.com/vllm-project/vllm/pull/44352)


### Base Information

- **PR Number:** #44352
- **Author:** [sfeng33](https://github.com/sfeng33)
- **Merged By:** [MatthewBonanni](https://github.com/MatthewBonanni)
- **Merged time:** 2026-06-02 21:05:20
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44352/files) (2):**
  - `.buildkite/test_areas/misc.yaml`
  - `tests/parser/test_parse.py`

### Summary

**What changed and why**  
Added vllm/parser/ to CI trigger paths in .buildkite/test_areas/misc.yaml so that CI runs when parser source code changes. Fixed test_parse.py by replacing usage of removed _WrappedParser with a new TestParser subclass of DelegatingParser, because _WrappedParser was removed in a previous PR and the test was failing.

**Technical impact**  
Ensures CI jobs for parser tests are triggered when parser source files change, preventing silent test failures. The test fix restores correctness of the test suite by adapting to the refactored parser architecture.

**Potential risks**  
Low risk. The CI trigger change only adds a path, no negative impact. The test fix is straightforward and follows the pattern used in other tests (e.g., test_streaming.py).

**Key insights**  
The original issue was a missing CI trigger path, which allowed a test to break silently. This highlights the importance of keeping CI trigger paths in sync with source code structure. The test fix demonstrates a clean way to subclass DelegatingParser for testing purposes.

---

## 9. [[CI] Reject out-of-vocabulary  before they reach the GPU logprob path](https://github.com/vllm-project/vllm/pull/44042)


### Base Information

- **PR Number:** #44042
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged time:** 2026-06-02 20:27:52
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44042/files) (4):**
  - `tests/v1/sample/test_logprobs.py`
  - `vllm/sampling_params.py`
  - `vllm/v1/sample/thinking_budget_state.py`
  - `vllm/v1/worker/gpu_model_runner.py`

### Summary

**What changed and why**  
Added validation in SamplingParams.verify() to reject logprob_token_ids that are out of vocabulary (negative or >= vocab_size). Also added ROCm-specific workarounds in thinking_budget_state.py (flattened index_fill) and gpu_model_runner.py (graph cleanup and cache clearing) to prevent HSA hardware faults. The changes fix a flaky CI failure where invalid token IDs caused GPU crashes on ROCm.

**Technical impact**  
Prevents invalid token IDs from reaching GPU logprob path, stabilizing ROCm CI. Introduces platform-specific code paths for ROCm to avoid known hardware issues with advanced indexing and graph destruction. Adds unit tests for the new validation.

**Potential risks**  
ROCm-specific workarounds may not be needed on future driver versions and could become dead code. The flattened index_fill approach assumes contiguous logits; if non-contiguous, falls back to element-wise assignment which may be slower. The graph cleanup and cache clearing in shutdown() could affect performance on ROCm but is necessary for stability.

**Key insights**  
The root cause was a missing validation that allowed out-of-vocab token IDs to trigger GPU hardware exceptions on ROCm. The fix combines input validation with platform-specific GPU workarounds, demonstrating a layered approach to handling flaky hardware issues.

---

## 10. [[ROCm][CI] Skip fp8 reload tests on gfx90a (MI250)](https://github.com/vllm-project/vllm/pull/44369)


### Base Information

- **PR Number:** #44369
- **Author:** [JartX](https://github.com/JartX)
- **Merged By:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged time:** 2026-06-02 20:27:14
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44369/files) (1):**
  - `tests/model_executor/model_loader/test_reload.py`

### Summary

**What changed and why**  
Added a helper function `_fp8_reload_unsupported()` that checks both `supports_fp8()` and a gfx90a guard for ROCm, and replaced direct `supports_fp8()` calls in three FP8-related tests with this new function. This ensures tests are skipped on MI250 (gfx90a) even though `supports_fp8()` returns True due to upcast paths, because MI250 lacks native FP8 and cannot run these models.

**Technical impact**  
Changes test behavior on ROCm gfx90a (MI250) by skipping FP8 reload, KV-scale reload, and online quantize reload tests that previously would run and likely fail. No impact on other platforms or non-FP8 tests.

**Potential risks**  
Low risk. The change is isolated to test skipping logic and only affects ROCm gfx90a. The helper function is well-defined and the existing `supports_fp8()` behavior is preserved for other platforms.

**Key insights**  
The PR addresses a platform-specific discrepancy where `supports_fp8()` returns True for MI250 due to upcast support, but the hardware cannot actually run FP8 models. The fix adds an explicit gfx90a check without modifying the general `supports_fp8()` API, which is a clean approach.

---

## 11. [[ROCm][CI] Fix stale wvSplitK GEMM fallback test for N=5](https://github.com/vllm-project/vllm/pull/44368)


### Base Information

- **PR Number:** #44368
- **Author:** [JartX](https://github.com/JartX)
- **Merged By:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged time:** 2026-06-02 20:00:25
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44368/files) (1):**
  - `tests/model_executor/layers/test_rocm_unquantized_gemm.py`

### Summary

**What changed and why**  
Updated a test to reflect a new wvSplitK cutoff: changed n from 5 to 6 and renamed the test from _n_gt_4_falls_back to _n_gt_5_falls_back. The previous test used n=5, which is now handled by wvSplitK, so it no longer falls back. The change ensures the test correctly verifies the fallback boundary.

**Technical impact**  
Fixes a failing CI test on ROCm platforms. The test now correctly validates that for n > 5, the GEMM falls back to torch.nn.functional.linear, matching the updated implementation.

**Potential risks**  
Low risk. The change is limited to a single test file and only adjusts test parameters and naming. No production code is affected.

**Key insights**  
The PR demonstrates the importance of keeping tests in sync with implementation changes, especially when cutoffs are adjusted. The test name and input size were both updated to match the new boundary, ensuring accurate coverage.

---

## 12. [[Platform] Add is_cumem_allocator_available](https://github.com/vllm-project/vllm/pull/43838)


### Base Information

- **PR Number:** #43838
- **Author:** [wangxiyuan](https://github.com/wangxiyuan)
- **Merged By:** [ZJY0516](https://github.com/ZJY0516)
- **Merged time:** 2026-06-02 19:54:50
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/43838/files) (2):**
  - `vllm/config/model.py`
  - `vllm/platforms/interface.py`

### Summary

**What changed and why**  
Moved the `is_cumem_allocator_available` function from `vllm/config/model.py` to the platform interface (`vllm/platforms/interface.py`) as a method of the platform class. This allows custom platforms to override the method and enable the cumem allocator feature. The call site in `model.py` was updated to use `current_platform.is_cumem_allocator_available()` instead of the standalone function.

**Technical impact**  
The change enables custom platform implementations to control whether the cumem allocator is available, improving extensibility. The behavior for existing platforms (CUDA, ROCM) remains unchanged because the default implementation in the base platform class retains the original logic.

**Potential risks**  
If a custom platform does not override `is_cumem_allocator_available`, it will use the default implementation which checks for the cumem module. This is safe. However, any code that previously imported the standalone function will break; since the function was not part of the public API (no imports in other files), the risk is low.

**Key insights**  
This is a refactoring that moves platform-specific logic to the platform interface, following the pattern of other platform methods like `is_sleep_mode_available`. It improves modularity and allows custom platforms to enable the cumem allocator without modifying core code.

---

## 13. [[docker] Stop using extra-index-url for flashinfer-jit-cache](https://github.com/vllm-project/vllm/pull/44366)


### Base Information

- **PR Number:** #44366
- **Author:** [khluu](https://github.com/khluu)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-06-02 18:58:23
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44366/files) (1):**
  - `docker/Dockerfile`

### Summary

**What changed and why**  
Changed the pip install flag from --extra-index-url to --index-url for flashinfer-jit-cache because the package is quarantined on PyPI, so using an extra index would still allow PyPI to be searched first, potentially causing issues. Using --index-url ensures only the specified index is used.

**Technical impact**  
This change ensures that flashinfer-jit-cache is installed exclusively from the flashinfer.ai index, avoiding any conflicts or failures due to PyPI quarantine. It may affect dependency resolution if other packages are not available on that index, but since this is a specific package install, impact is limited.

**Potential risks**  
If the flashinfer.ai index is unavailable or slow, the build may fail. Also, if other dependencies of flashinfer-jit-cache are not on that index, they won't be found, but typically such packages are self-contained.

**Key insights**  
The change is a targeted fix for a PyPI quarantine issue, switching from extra index to primary index for a specific package. It's a simple one-line change with clear reasoning.

---

## 14. [[Bugfix] Fix Deepseek v4 non-mega-moe model init error](https://github.com/vllm-project/vllm/pull/44356)


### Base Information

- **PR Number:** #44356
- **Author:** [wzhao18](https://github.com/wzhao18)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-06-02 18:26:31
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44356/files) (1):**
  - `vllm/models/deepseek_v4/nvidia/model.py`

### Summary

**What changed and why**  
Added missing attribute initialization in `_init_fused_moe_experts` for Deepseek V4 non-mega-moe models. The attributes `n_redundant_experts`, `n_shared_experts`, `n_logical_experts`, `n_physical_experts`, `n_local_physical_experts`, `physical_expert_start`, and `physical_expert_end` were previously only set in `_init_mega_moe_experts`, causing an AttributeError when `extract_moe_parameters` tried to access `n_logical_experts` on non-mega-moe models.

**Technical impact**  
Fixes a runtime crash during model initialization for Deepseek V4 models that do not use mega-moe configuration. The error occurred after a prior PR updated only the mega-moe path, leaving the fused moe path incomplete. This change ensures both initialization paths set the same required attributes, restoring functionality for non-mega-moe models.

**Potential risks**  
Low risk. The change only adds attribute assignments that mirror those already present in the mega-moe path. No existing logic is altered, and the attributes are used consistently elsewhere. The fix is straightforward and matches the pattern already established.

**Key insights**  
The bug was introduced by an incomplete refactor that updated only one of two initialization paths. This highlights the importance of ensuring parallel code paths are updated consistently. The fix is minimal and directly addresses the root cause by adding the missing attributes.

---

## 15. [[Perf] Apply single-pass min_larger finding and binary search in Triton Top-p path.](https://github.com/vllm-project/vllm/pull/42191)


### Base Information

- **PR Number:** #42191
- **Author:** [cakeng](https://github.com/cakeng)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-06-02 17:57:27
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/42191/files) (1):**
  - `vllm/v1/sample/ops/topk_topp_triton.py`

### Summary

**What changed and why**  
This PR applies a single-pass min_larger finding helper function to the Top-p path of the Triton top-k, top-p kernel, replacing the previous two-pass approach. To reduce register pressure, the search algorithm is changed from ternary to binary. Additionally, bugs in logit calculation and masking are fixed.

**Technical impact**  
The change yields a 25-40% speedup in the Top-p path across various batch sizes and vocab sizes, as shown in benchmark results. The code is simplified by removing one pivot point and fusing two passes into one, reducing the number of loops and memory accesses. The helper function `_update_min_larger_stats` is introduced to handle tile-level merge logic.

**Potential risks**  
The binary search may converge differently than ternary, potentially affecting correctness in edge cases. The helper function increases register pressure, which could impact performance on devices with limited registers. The bug fixes may introduce new issues if the original logic was relied upon. However, all correctness tests passed.

**Key insights**  
The PR demonstrates that a single-pass approach with binary search can outperform a two-pass ternary search by reducing memory traffic and loop overhead. The use of a helper function for min_larger stats enables code reuse and clarity. The benchmark results show consistent speedups, especially for larger batch sizes, indicating the change is beneficial for production workloads.

---

## 16. [[DSV4] Minor cleanup for DeepseekV4MegaMoEExperts](https://github.com/vllm-project/vllm/pull/44367)


### Base Information

- **PR Number:** #44367
- **Author:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-06-02 17:54:28
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44367/files) (1):**
  - `vllm/models/deepseek_v4/nvidia/model.py`

### Summary

**What changed and why**  
Removed the private method `_run_mega_moe` and inlined its logic directly into the `forward` method. This eliminates an unnecessary indirection and simplifies the code, as the method was only called once.

**Technical impact**  
Reduces code complexity by removing a redundant wrapper method. The behavior remains identical because the same deep_gemm operations are executed in the same order. The change is purely structural with no functional difference.

**Potential risks**  
Low risk. The inlined code is a direct copy of the original method body, so no logic changes. However, any future modifications to the MoE logic would need to be done in `forward` instead of a separate method, which could slightly reduce modularity.

**Key insights**  
The removal of `_run_mega_moe` is a minor cleanup that improves readability by reducing indirection. It's a safe refactor since the method was only used once and had no external dependencies.

---

## 17. [[Misc] Remove dead VLLM_RPC_TIMEOUT env var and fix profiling doc that references it](https://github.com/vllm-project/vllm/pull/44128)


### Base Information

- **PR Number:** #44128
- **Author:** [DaoyuanLi2816](https://github.com/DaoyuanLi2816)
- **Merged By:** [sfeng33](https://github.com/sfeng33)
- **Merged time:** 2026-06-02 17:22:11
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44128/files) (11):**
  - `.buildkite/performance-benchmarks/tests/latency-tests-arm64-cpu.json`
  - `.buildkite/performance-benchmarks/tests/latency-tests-cpu.json`
  - `.buildkite/performance-benchmarks/tests/serving-tests-arm64-cpu.json`
  - `.buildkite/performance-benchmarks/tests/serving-tests-cpu-asr.json`
  - `.buildkite/performance-benchmarks/tests/serving-tests-cpu-embed.json`
  - `.buildkite/performance-benchmarks/tests/serving-tests-cpu-text.json`
  - `.buildkite/performance-benchmarks/tests/serving-tests-cpu.json`
  - `.buildkite/performance-benchmarks/tests/throughput-tests-arm64-cpu.json`
  - `.buildkite/performance-benchmarks/tests/throughput-tests-cpu.json`
  - `docs/contributing/profiling.md`
  - `vllm/envs.py`

### Summary

**What changed and why**  
Removed the dead VLLM_RPC_TIMEOUT environment variable from vllm/envs.py and all references in .buildkite performance benchmark configs, and updated the profiling documentation to remove the obsolete instruction to set that variable. The variable had no consumers in V1 and was a leftover from V0.

**Technical impact**  
Reduces code clutter and removes a misleading env var that had no effect. The profiling doc now accurately states that the engine client waits without timeout for trace flush to complete. No runtime behavior changes since the variable was already unused.

**Potential risks**  
Very low risk. Any user still exporting VLLM_RPC_TIMEOUT will simply have an unused environment variable, same as before. No functional code paths are affected.

**Key insights**  
The PR cleans up dead code and documentation that could mislead users into thinking they could control a timeout that didn't exist. It also removes the variable from CI benchmark configs, simplifying those files. The change is safe and well-justified with thorough verification of zero consumers.

---

## 18. [[MoE/b12x] Accept W4A16 (kNvfp4Static, None) in FlashInferB12xExperts supports check](https://github.com/vllm-project/vllm/pull/43332)


### Base Information

- **PR Number:** #43332
- **Author:** [ECMGit](https://github.com/ECMGit)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-06-02 15:20:38
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/43332/files) (1):**
  - `vllm/model_executor/layers/fused_moe/experts/flashinfer_b12x_moe.py`

### Summary

**What changed and why**  
Modified FlashInferB12xExperts._supports_quant_scheme to accept (kNvfp4Static, None) in addition to (kNvfp4Static, kNvfp4Dynamic), enabling W4A16 NVFP4 checkpoints (activation_key=None) to use the b12x kernel. Also added _fc2_input_scale tensor to handle missing a2_gscale for W4A16 checkpoints, ensuring alloc-free apply().

**Technical impact**  
Allows W4A16 NVFP4 checkpoints (e.g., nvidia/Qwen3.6-35B-A3B-2.06GB-per-token) to use the FlashInfer b12x MoE backend instead of falling back to Marlin. The change is a metadata gate loosening plus a small allocation in process_weights_after_loading; no kernel modifications. Performance is on par with Marlin (91 tok/s vs 92 tok/s).

**Potential risks**  
Low risk. The existing (kNvfp4Static, kNvfp4Dynamic) path is unchanged. The new _fc2_input_scale tensor is allocated once and reused, so no runtime overhead. The change only affects the dispatcher's acceptance logic and a single tensor initialization; no new dependencies or kernel changes.

**Key insights**  
The b12x kernel already supports BF16 activation input via in-kernel quantization, so the dispatcher gate was overly restrictive. This PR complements PR #42566 by fixing the b12x side rather than routing W4A16 to Marlin. The test results show successful serving with 91 tok/s throughput and zero errors.

---

## 19. [[CI/Build] Bump flashinfer to v0.6.12](https://github.com/vllm-project/vllm/pull/44036)


### Base Information

- **PR Number:** #44036
- **Author:** [vadiklyutiy](https://github.com/vadiklyutiy)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-06-02 15:19:06
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44036/files) (4):**
  - `docker/Dockerfile`
  - `docker/Dockerfile.nightly_torch`
  - `docker/versions.json`
  - `requirements/cuda.txt`

### Summary

**What changed and why**  
Updated FlashInfer dependency from v0.6.11.post2 to v0.6.12 across Dockerfiles, versions.json, and requirements/cuda.txt. This is a routine version bump to incorporate upstream bug fixes and improvements.

**Technical impact**  
All builds and runtime environments using FlashInfer will now use the newer version. This may affect performance or correctness of attention kernels, but the change is minor and expected to be backward-compatible.

**Potential risks**  
Low risk. The version bump is a patch-level update (0.6.11.post2 -> 0.6.12). Potential incompatibilities are unlikely but could arise if the new version changes API or behavior. The change is isolated to dependency version strings.

**Key insights**  
The PR is a straightforward dependency version bump with no code logic changes. It ensures the project uses the latest stable FlashInfer release, which may include bug fixes or minor improvements.

---

## 20. [[BugFix] Fix sparse NCCL weight transfer test construction](https://github.com/vllm-project/vllm/pull/44345)


### Base Information

- **PR Number:** #44345
- **Author:** [bedeks](https://github.com/bedeks)
- **Merged By:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged time:** 2026-06-02 14:51:22
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44345/files) (2):**
  - `docs/training/weight_transfer/base.md`
  - `tests/distributed/test_weight_transfer.py`

### Summary

**What changed and why**  
Updated two files to pass a model argument to NCCLWeightTransferEngine constructor, fixing a breakage caused by a production change that added a required model parameter. The test and documentation were constructing the engine directly without the model argument, causing failures in sparse NCCL weight transfer tests.

**Technical impact**  
Fixes failing tests in nightly CI for sparse NCCL weight transfer paths. Ensures that test and example code aligns with the updated constructor signature, preventing runtime errors when running on multi-GPU setups.

**Potential risks**  
Low risk. The change is minimal and only adds a required argument (MagicMock in tests, actual model in docs) to match the production API. No behavioral changes to production code.

**Key insights**  
The bug was caused by stale test code not updated alongside the production change. This highlights the importance of keeping test and example code in sync with API changes, especially for constructor signatures.

---

## 21. [[Kernel][MoE] Add GELU_TANH to CPU, CUTLASS, and WNA16 MoE backends](https://github.com/vllm-project/vllm/pull/42027)


### Base Information

- **PR Number:** #42027
- **Author:** [lesj0610](https://github.com/lesj0610)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-06-02 14:12:09
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/42027/files) (7):**
  - `csrc/cpu/cpu_fused_moe.cpp`
  - `tests/kernels/moe/test_cpu_fused_moe.py`
  - `tests/kernels/moe/test_cutlass_moe.py`
  - `tests/quantization/test_moe_wna16.py`
  - `vllm/model_executor/layers/fused_moe/cpu_fused_moe.py`
  - `vllm/model_executor/layers/fused_moe/experts/cutlass_moe.py`
  - `vllm/model_executor/layers/quantization/moe_wna16.py`

### Summary

**What changed and why**  
Added GELU_TANH activation support to CPU fused MoE, CUTLASS FP8/FP4, and WNA16 MoE backends. Previously these backends rejected GELU_TANH, causing runtime errors or unnecessary fallbacks. The changes include adding the activation enum and kernel in C++ CPU code, updating activation lists in Python, removing a SiLU-only assert in WNA16, and adding tests.

**Technical impact**  
Enables GELU_TANH activation for MoE layers across CPU, CUTLASS, and WNA16 backends, expanding model compatibility. The CPU backend now has a vectorized GELU_TANH kernel. CUTLASS backends correctly report support, avoiding fallback. WNA16 no longer crashes on non-SiLU activations.

**Potential risks**  
New CPU kernel may have performance or numerical accuracy issues; tests only cover basic correctness. CUTLASS support is metadata-only; actual kernel execution is not tested. WNA16 change removes a safety check, potentially allowing unsupported activations to pass through to fused_experts, which may fail elsewhere.

**Key insights**  
The PR addresses a gap where core activation support existed but backend-specific code was missing. The CPU kernel uses a tanh approximation for GELU, matching PyTorch's 'tanh' approximate mode. The WNA16 change is minimal but critical for non-SiLU models. Tests are added for each backend, though CUTLASS test only checks metadata.

---

## 22. [[ModelRunnerV2] Avoid pipeline parallel bubbles](https://github.com/vllm-project/vllm/pull/42187)


### Base Information

- **PR Number:** #42187
- **Author:** [njhill](https://github.com/njhill)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-06-02 14:02:02
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/42187/files) (19):**
  - `.buildkite/test_areas/model_runner_v2.yaml`
  - `tests/v1/distributed/test_pp_dp_v2.py`
  - `tests/v1/worker/test_gpu_model_runner_v2_eplb.py`
  - `vllm/config/vllm.py`
  - `vllm/distributed/parallel_state.py`
  - `vllm/v1/core/sched/async_scheduler.py`
  - `vllm/v1/core/sched/scheduler.py`
  - `vllm/v1/engine/core.py`
  - `vllm/v1/request.py`
  - `vllm/v1/worker/gpu/buffer_utils.py`
  - `vllm/v1/worker/gpu/input_batch.py`
  - `vllm/v1/worker/gpu/model_runner.py`
  - `vllm/v1/worker/gpu/model_states/default.py`
  - `vllm/v1/worker/gpu/model_states/interface.py`
  - `vllm/v1/worker/gpu/model_states/mamba_hybrid.py`
  - `vllm/v1/worker/gpu/model_states/whisper.py`
  - `vllm/v1/worker/gpu/pp_utils.py`
  - `vllm/v1/worker/gpu/sample/prompt_logprob.py`
  - `vllm/v1/worker/gpu/states.py`

### Summary

**What changed and why**  
Reorganized PP scheduling and broadcast of sampled tokens to avoid pipeline bubbles. Decode tokens are now scheduled every pp_size steps, broadcast uses a separate stream and process group to overlap with p2p comms, and prefill chunks are pipelined consecutively. This fixes an IMA and improves throughput and TTFT significantly.

**Technical impact**  
Introduces PPHandler class for async broadcast/recv on a side stream with a dedicated NCCL communicator. Scheduler enforces pp_size cadence via next_decode_eligible_step. UVA buffer pool concurrency is set to max_concurrent_batches. New tests cover PP+DP integration with various concurrency and abort scenarios.

**Potential risks**  
Increased complexity in scheduling and broadcast logic may introduce subtle race conditions or deadlocks. The new broadcast group creation (make_sibling_device_group) is a collective call that must be invoked by all ranks. The change to max_concurrent_batches for async scheduling with V2 model runner could affect memory usage. The test file requires 4 GPUs and VLLM_USE_V2_MODEL_RUNNER=1, limiting test coverage.

**Key insights**  
The key insight is to decouple the broadcast of sampled tokens from the main pipeline by using a separate stream and process group, allowing it to overlap with subsequent p2p communications. Scheduling decode steps every pp_size steps ensures the broadcast slot ring stays consistent. The animation and benchmarks demonstrate significant performance gains (up to 3.17x throughput improvement).

---

## 23. [[Misc] Remove stray empty file](https://github.com/vllm-project/vllm/pull/44350)


### Base Information

- **PR Number:** #44350
- **Author:** [MatthewBonanni](https://github.com/MatthewBonanni)
- **Merged By:** [MatthewBonanni](https://github.com/MatthewBonanni)
- **Merged time:** 2026-06-02 12:53:03
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44350/files) (1):**
  - `=4.5.1`

### Summary

**What changed and why**  
Removed a stray empty file that was accidentally introduced by a previous pull request (#43754). The file had no content and served no purpose.

**Technical impact**  
No impact on functionality, build, or runtime behavior. Only cleans up the repository by deleting an unintended file.

**Potential risks**  
None. The file was empty and unused, so removal cannot cause any issues.

**Key insights**  
This is a trivial cleanup to maintain repository hygiene. The change is safe and has zero effect on the codebase.

---

## 24. [[Bugfix] Cache the EAGLE/MTP lookahead block in the SWA prefix-cache mask](https://github.com/vllm-project/vllm/pull/44082)


### Base Information

- **PR Number:** #44082
- **Author:** [ivanium](https://github.com/ivanium)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-06-02 12:21:08
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44082/files) (5):**
  - `tests/v1/core/test_prefix_caching.py`
  - `tests/v1/core/test_single_type_kv_cache_manager.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/mooncake/store/coordinator.py`
  - `vllm/v1/core/kv_cache_coordinator.py`
  - `vllm/v1/core/single_type_kv_cache_manager.py`

### Summary

**What changed and why**  
Fixed a bug where the SWA prefix-cache mask was too aggressive when EAGLE/MTP speculative decoding is active and the cache-hit alignment is larger than the SWA window. The mask skipped the lookahead block needed by EAGLE, causing no prefix-cache hits. The fix caches that extra block while preserving the memory-saving optimization from PR #42258. Changes include refactoring the contiguous-blocks calculation into a shared helper, reworking the reachable block mask to include the EAGLE lookahead block, extending num_tokens_to_cache in HybridKVCacheCoordinator for EAGLE groups, introducing a SpecGroup NamedTuple to propagate the use_eagle bit, and renaming use_eagle to drop_eagle_block in find_longest_cache_hit for clarity.

**Technical impact**  
Fixes a critical regression where EAGLE + SWA with sliding_window <= alignment_tokens would find zero prefix-cache hits, degrading speculative decoding performance. The fix is minimal: only one extra block per aligned segment is cached, so the memory benefit of the original mask is largely retained. The refactoring into a shared helper ensures the mask and lookup logic stay in sync, preventing similar bugs. The SpecGroup NamedTuple and use_eagle propagation improve code clarity and correctness for grouped SWA siblings.

**Potential risks**  
The change touches core caching logic in multiple files (5 files, +338/-82 lines). The new reachable_block_mask logic and the extended num_tokens_to_cache could introduce off-by-one errors or alignment issues, especially for edge cases like block_size != alignment_tokens or non-EAGLE groups. The rename of use_eagle to drop_eagle_block in find_longest_cache_hit may cause merge conflicts with other PRs. The MooncakeStoreConnector fix is deferred, so that connector may still have the bug. Tests cover the main scenarios but may miss some corner cases.

**Key insights**  
The bug stemmed from a mismatch between the cache mask and the cache-hit lookup: the mask assumed only tail blocks were needed, but EAGLE requires tail+1 contiguous blocks. The fix uses a shared helper (_contiguous_blocks_for_hit) to derive both the mask and the lookup from the same logic, ensuring consistency. The SpecGroup NamedTuple elegantly bundles spec, group_ids, manager_cls, and use_eagle, replacing ad-hoc sets and improving maintainability. The rename from use_eagle to drop_eagle_block clarifies that the parameter controls whether to drop the last matched block, not whether the group uses EAGLE.

---

## 25. [[MRV2] Remove assignment of graph_pool in cudagraph_utils](https://github.com/vllm-project/vllm/pull/44338)


### Base Information

- **PR Number:** #44338
- **Author:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-06-02 11:50:27
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44338/files) (1):**
  - `vllm/v1/worker/gpu/cudagraph_utils.py`

### Summary

**What changed and why**  
Removed a redundant assignment of graph_pool in cudagraph_utils.py. The line was unnecessary because both the removed assignment and the default use current_platform.get_global_graph_pool(). This is a follow-up cleanup from PR #44078.

**Technical impact**  
No behavioral change. The graph_pool attribute was already set to the same value by default, so removing the explicit assignment has no effect on runtime behavior.

**Potential risks**  
None. The change is purely cosmetic and removes dead code. It does not affect any logic or performance.

**Key insights**  
This is a minor cleanup that reduces code duplication and potential confusion. It demonstrates good practice of removing redundant assignments that are already handled by default initialization.

---

## 26. [Bump actions/github-script from 8.0.0 to 9.0.0](https://github.com/vllm-project/vllm/pull/39667)


### Base Information

- **PR Number:** #39667
- **Author:** [dependabot[bot]](https://github.com/dependabot[bot])
- **Merged By:** [njhill](https://github.com/njhill)
- **Merged time:** 2026-06-02 11:26:57
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39667/files) (4):**
  - `.github/workflows/add_label_automerge.yml`
  - `.github/workflows/issue_autolabel.yml`
  - `.github/workflows/new_pr_bot.yml`
  - `.github/workflows/pre-commit.yml`

### Summary

**What changed and why**  
Updated actions/github-script from v8.0.0 to v9.0.0 across four workflow files. This major version introduces a new getOctokit factory function, adds orchestration ID to user-agent, and upgrades to @actions/github v9 (ESM-only), which breaks require('@actions/github') usage. The update is performed by Dependabot to keep dependencies current.

**Technical impact**  
The change updates the GitHub Action to a new major version with breaking changes. Scripts using require('@actions/github') will fail; they must use the injected getOctokit function instead. The new getOctokit function enables creating additional authenticated Octokit clients. The orchestration ID improves request tracing. All four workflow files are updated to use the new version commit hash.

**Potential risks**  
Workflows that rely on require('@actions/github') will break at runtime. Scripts that redeclare getOctokit with const/let will cause SyntaxError. Any usage of other @actions/github internals may need updates. The compatibility score is not shown, so there is a risk of unexpected failures if scripts depend on removed or changed APIs.

**Key insights**  
This is a major version bump with breaking changes, but the PR only updates the version references without modifying the scripts themselves. The scripts in these workflows do not appear to use require('@actions/github') or redeclare getOctokit, so they are likely compatible. However, thorough testing is recommended to ensure no runtime errors occur.

---

## 27. [[MRV2] Also enable MRV2 for Llama and Mistral dense models](https://github.com/vllm-project/vllm/pull/43458)


### Base Information

- **PR Number:** #43458
- **Author:** [njhill](https://github.com/njhill)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-06-02 11:18:46
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/43458/files) (5):**
  - `tests/test_config.py`
  - `tests/v1/engine/test_abort_final_step.py`
  - `tests/v1/shutdown/test_forward_error.py`
  - `vllm/config/vllm.py`
  - `vllm/v1/worker/gpu/model_runner.py`

### Summary

**What changed and why**  
Enabled MRV2 (Model Runner V2) for Llama and Mistral dense models by adding their architectures to the default set. Also fixed test failures by adjusting test logic for V2's warmup calls and adding a missing parameter in a test mock. These changes extend V2 coverage to more models and ensure CI passes.

**Technical impact**  
Llama and Mistral models will now use the V2 model runner by default, which may affect performance and behavior. The test changes ensure that existing tests work with V2's different execution flow (e.g., warmup calls). The sparse weight patches method was added to V2 runner to maintain compatibility.

**Potential risks**  
Enabling V2 for new architectures may introduce regressions or unexpected behavior in those models. The test modifications rely on internal V2 details (warmup calls) which could change. The sparse weight patches delegation might not cover all cases if V1 and V2 diverge further.

**Key insights**  
The PR combines a previous effort with fixes for CI failures, showing iterative development. The test changes reveal that V2's engine init calls execute_model multiple times for warmup, which required careful filtering to avoid stalling on unrelated requests. The addition of intermediate_tensors parameter in a test mock highlights a V2 API difference.

---

## 28. [[Anthropic] Support system role messages inside messages array](https://github.com/vllm-project/vllm/pull/44283)


### Base Information

- **PR Number:** #44283
- **Author:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged By:** [sfeng33](https://github.com/sfeng33)
- **Merged time:** 2026-06-02 11:13:55
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44283/files) (3):**
  - `tests/entrypoints/anthropic/test_anthropic_messages_conversion.py`
  - `vllm/entrypoints/anthropic/protocol.py`
  - `vllm/entrypoints/anthropic/serving.py`

### Summary

**What changed and why**  
Added support for system role messages inside the messages array in the Anthropic API adapter. Previously, only the top-level 'system' field was accepted; now inline system messages are merged with the top-level system prompt. This fixes issue #44000 where clients that place system messages inside the messages array were not handled.

**Technical impact**  
Changes the Anthropic message conversion logic in `serving.py` to collect system content from both the top-level `system` field and any messages with role 'system' in the messages array, then prepend a single system message to the OpenAI messages. The `AnthropicMessage` model's role field was updated to allow 'system' in addition to 'user' and 'assistant'. The `_convert_messages` method now skips system messages to avoid duplication. Tests were added covering various combinations of inline and top-level system messages.

**Potential risks**  
If clients send both top-level system and inline system messages, they are concatenated without separators, which might change behavior for some users. The billing header stripping logic is applied to both sources, which is consistent but could affect users relying on that header. The change assumes that system messages should always be merged into a single system message at the beginning; this might not match all client expectations.

**Key insights**  
The PR addresses a common pattern where clients embed system prompts in the messages array instead of using the dedicated field, improving compatibility with non-standard Anthropic API usage. The implementation is straightforward: collect all system text parts, concatenate them, and prepend as a single system message. The test coverage is thorough, including edge cases like multiple inline system messages and mixed content types.

---

## 29. [[Feature] Support EPLB for DeepSeek v4 Mega Moe](https://github.com/vllm-project/vllm/pull/43339)


### Base Information

- **PR Number:** #43339
- **Author:** [wzhao18](https://github.com/wzhao18)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-06-02 10:56:44
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/43339/files) (4):**
  - `vllm/distributed/eplb/eplb_utils.py`
  - `vllm/models/deepseek_v4/nvidia/model.py`
  - `vllm/utils/deep_gemm.py`
  - `vllm/v1/worker/gpu_worker.py`

### Summary

**What changed and why**  
Added support for Expert Parallel Load Balancing (EPLB) for DeepSeek V4 Mega MoE model. Changes include: (1) extended override_envs_for_eplb to also set NCCL_MAX_CTAS for deep_gemm_mega_moe backend to prevent hangs due to cooperative launch contention; (2) refactored DeepSeekV4MoE layer to support logical-to-physical expert mapping, weight loading for replicated experts, and EPLB state integration; (3) added get_expert_weights and update_expert_map methods for EPLB weight exchange; (4) cached _import_deep_gemm import; (5) passed moe_backend to override_envs_for_eplb in GPU worker.

**Technical impact**  
Enables EPLB for DeepSeek V4 Mega MoE, improving load balancing across data-parallel ranks. Performance results show ~5% throughput improvement (1288 to 1351 tok/s) and reduced latency (TTFT mean 1694 to 1520 ms, TPOT mean 96.1 to 91.7 ms). The change modifies the MoE layer to handle replicated experts and integrates with the EPLB framework for dynamic expert mapping.

**Potential risks**  
The NCCL_MAX_CTAS override now applies to deep_gemm_mega_moe even without async EPLB, which may affect performance on other configurations. The weight loading logic now supports multiple local copies per logical expert, which could introduce subtle bugs if the mapping is incorrect. The static_forward_context registration may cause issues if layer names are duplicated. The get_expert_weights method assumes specific tensor layouts; unexpected strides could raise errors.

**Key insights**  
The hang issue with EPLB and cooperative MoE backends (DeepEP low-latency and DeepGEMM Mega MoE) is resolved by limiting NCCL occupancy via NCCL_MAX_CTAS=8. The implementation reuses existing EPLB infrastructure (eplb_map_to_physical_and_record) and integrates with the layer's forward pass. The weight loader now handles multiple physical copies per logical expert, enabling expert replication for load balancing.

---

## 30. [[Bugfix] flashinfer: fail fast when --kv-cache-dtype nvfp4 used on unsupported arch](https://github.com/vllm-project/vllm/pull/43669)


### Base Information

- **PR Number:** #43669
- **Author:** [Kartavyasonar](https://github.com/Kartavyasonar)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-06-02 10:50:00
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/43669/files) (1):**
  - `vllm/v1/attention/backends/flashinfer.py`

### Summary

**What changed and why**  
Added a capability check in flashinfer.py that raises a ValueError at engine initialization if --kv-cache-dtype nvfp4 is used on a GPU architecture that does not support trtllm-gen FP4 FMHA kernels (sm_100/sm_103). Previously, the engine would start successfully but crash on the first request with an obscure error, making debugging difficult. This change fails fast with a clear message.

**Technical impact**  
Prevents silent failures and improves user experience by catching unsupported hardware configurations early. The check uses existing infrastructure (current_platform.is_device_capability_family) and adds minimal overhead (7 lines). No behavioral change for supported architectures.

**Potential risks**  
Low risk. The change is small and uses an already-imported utility function. Potential edge case: if the capability family check is too broad or narrow, it could incorrectly allow or block certain architectures. However, the check targets sm100f family which is well-defined for FP4 support.

**Key insights**  
The root cause was that vLLM forced the trtllm-gen backend for nvfp4 without verifying hardware support. This fix adds a simple guard that aligns with the existing pattern of capability checks in the codebase. The error message is actionable, suggesting an alternative dtype.

---

## 31. [[BugFix] Fix Humming MoE deploy error](https://github.com/vllm-project/vllm/pull/43100)


### Base Information

- **PR Number:** #43100
- **Author:** [adotdad](https://github.com/adotdad)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-06-02 09:32:51
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/43100/files) (1):**
  - `vllm/model_executor/layers/quantization/humming.py`

### Summary

**What changed and why**  
Fixed two bugs in Humming MoE deployment: (1) Added call to ensure_moe_quant_config_init() before assertion to initialize moe_quant_config, which was never set. (2) Moved schema allocation (weight_schemas, input_schemas) outside the conditional block so they are initialized even for Humming-formatted checkpoints, preventing empty schemas.

**Technical impact**  
Enables successful deployment of Humming packed quantization models (e.g., ISTA-DASLab/Qwen3.6-35B-A3B-2Bit-GSQ) that previously failed with assertion errors or empty schemas. The fix ensures proper initialization of quantization config and layer schemas, allowing the model to run correctly.

**Potential risks**  
Low risk. Changes are minimal (+3/-2 lines) and directly address the reported bugs. The moved schema allocation now runs unconditionally, which is safe because the variables are always defined. The added method call is a no-op if already initialized. No side effects on other quantization methods.

**Key insights**  
The bugs were caused by missing initialization paths for Humming-specific configurations when loading pre-quantized checkpoints. The fixes ensure that critical data structures (moe_quant_config, weight_schemas, input_schemas) are always populated regardless of checkpoint format, aligning with the expected behavior.

---

## 32. [[XPU] Enable rms_norm/act quant fusions](https://github.com/vllm-project/vllm/pull/43963)


### Base Information

- **PR Number:** #43963
- **Author:** [zhenwei-intel](https://github.com/zhenwei-intel)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-06-02 09:14:42
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/43963/files) (4):**
  - `vllm/compilation/passes/fusion/act_quant_fusion.py`
  - `vllm/compilation/passes/fusion/rms_quant_fusion.py`
  - `vllm/compilation/passes/pass_manager.py`
  - `vllm/platforms/xpu.py`

### Summary

**What changed and why**  
Enabled rms_norm and activation quantization fusion passes for XPU by replacing hardcoded 'cuda' device with current_platform.device_type in empty tensor creation functions. Also added conditional import of these fusion passes for XPU and modified the warning logic to only disable unsupported fusions when compilation mode is not NONE, preventing warnings when compilation is disabled.

**Technical impact**  
These changes allow the RMSNorm and activation quantization fusion passes to work on XPU devices, improving performance by enabling graph optimizations. The device-agnostic empty tensor functions ensure compatibility across platforms. The warning suppression reduces noise for users not using compilation.

**Potential risks**  
The fusion passes may have XPU-specific issues not yet discovered, as they were previously disabled. The conditional import and warning logic changes could mask real incompatibilities if the passes are not fully functional on XPU. Testing on XPU hardware is needed to validate correctness and performance.

**Key insights**  
The PR demonstrates a pattern for enabling XPU support by replacing CUDA-specific device strings with platform-agnostic calls. The warning suppression is a UX improvement that avoids unnecessary alerts when compilation is off. The changes are minimal but enable significant functionality for XPU users.

---

## 33. [[Refactor] Remove dead code from parser infrastructure](https://github.com/vllm-project/vllm/pull/44279)


### Base Information

- **PR Number:** #44279
- **Author:** [sfeng33](https://github.com/sfeng33)
- **Merged By:** [sfeng33](https://github.com/sfeng33)
- **Merged time:** 2026-06-02 09:08:28
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44279/files) (5):**
  - `tests/parser/test_streaming.py`
  - `vllm/parser/__init__.py`
  - `vllm/parser/abstract_parser.py`
  - `vllm/parser/minimax_m2_parser.py`
  - `vllm/parser/parser_manager.py`

### Summary

**What changed and why**  
Removed dead code: deleted `_WrappedParser` class, `MiniMaxM2Parser` file, and unused `ParserManager` registry methods (`register_module`, `register_lazy_module`, `get_parser_internal`, `list_registered`, `import_parser`, lazy-registration table). Moved sub-parser instantiation into `Parser.__init__` and updated `get_parser` to create a local `DelegatingParser` subclass instead of mutating `_WrappedParser`. Updated test to use `DelegatingParser` directly. This simplifies the codebase by eliminating unused abstractions and reducing maintenance burden.

**Technical impact**  
Reduces code size by ~293 lines. The `ParserManager.get_parser` method now directly composes a `DelegatingParser` subclass instead of relying on a global mutable `_WrappedParser`. The `Parser.__init__` now accepts `tools` parameter and instantiates sub-parsers based on class attributes, making the base class self-contained. The `MiniMaxM2Parser` is removed; its functionality is already covered by the individual reasoning and tool parsers registered elsewhere. The lazy registration system is removed entirely, so any external code relying on `ParserManager.register_lazy_module` or `get_parser_internal` will break.

**Potential risks**  
External code that uses `ParserManager.register_lazy_module`, `get_parser_internal`, `list_registered`, or `import_parser` will fail. The `_WrappedParser` class was removed; any code that imported it directly (e.g., `from vllm.parser.abstract_parser import _WrappedParser`) will break. The test file was updated to use `DelegatingParser` instead, but other tests or integrations may still reference `_WrappedParser`. The removal of `MiniMaxM2Parser` could affect users who explicitly requested it by name, though it was likely unused since it was just a composition of already-registered parsers.

**Key insights**  
The refactor eliminates a significant amount of dead code that was adding complexity without clear benefit. The `_WrappedParser` pattern was replaced by moving instantiation logic into `Parser.__init__`, which is cleaner and more maintainable. The `ParserManager` is now a thin facade that composes parsers from existing registries, rather than a full registry with lazy loading. This change aligns with the principle of removing unused abstractions and reducing the surface area of the codebase.

---

## 34. [[Core] Move `max_concurrent_batches` to `VllmConfig`](https://github.com/vllm-project/vllm/pull/44274)


### Base Information

- **PR Number:** #44274
- **Author:** [njhill](https://github.com/njhill)
- **Merged By:** [njhill](https://github.com/njhill)
- **Merged time:** 2026-06-02 08:57:26
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44274/files) (11):**
  - `tests/distributed/test_multiproc_executor.py`
  - `tests/distributed/test_ray_v2_executor.py`
  - `tests/model_executor/model_loader/tensorizer_loader/conftest.py`
  - `tests/v1/engine/test_engine_core.py`
  - `tests/v1/engine/test_engine_core_client.py`
  - `vllm/config/vllm.py`
  - `vllm/v1/engine/core.py`
  - `vllm/v1/executor/abstract.py`
  - `vllm/v1/executor/multiproc_executor.py`
  - `vllm/v1/executor/ray_executor.py`
  - `vllm/v1/executor/uniproc_executor.py`

### Summary

**What changed and why**  
Moved the `max_concurrent_batches` property from individual executor classes (abstract, multiproc, ray, uniproc) to `VllmConfig` as a centralized property. The logic is now computed based on pipeline parallel size and async scheduling config, eliminating duplication and making it accessible to non-executor components like the V2 model runner.

**Technical impact**  
Reduces code duplication across four executor implementations. Centralizes configuration logic, making it easier to maintain and extend. The property is now available on `VllmConfig`, which is passed to the engine core and can be used by other components without needing an executor instance.

**Potential risks**  
The change is straightforward but could affect any code that directly accesses `executor.max_concurrent_batches`. All usages have been updated in the PR, but external or future code relying on the executor property would break. The logic is preserved exactly, so no behavioral change is expected.

**Key insights**  
This refactor aligns with the principle of moving configuration to a central config object. It also prepares for future use by the V2 model runner, which may not have direct access to an executor. The test updates show careful handling of mocking to maintain test coverage.

---

## 35. [[compressed-tensors] Asymmetric support for MoE WNA16 marlin](https://github.com/vllm-project/vllm/pull/44025)


### Base Information

- **PR Number:** #44025
- **Author:** [brian-dellabetta](https://github.com/brian-dellabetta)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-06-02 08:51:46
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44025/files) (5):**
  - `vllm/model_executor/layers/fused_moe/experts/marlin_moe.py`
  - `vllm/model_executor/layers/fused_moe/oracle/int_wna16.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/compressed_tensors_moe/compressed_tensors_moe_wna16_marlin.py`
  - `vllm/model_executor/layers/quantization/utils/marlin_utils.py`
  - `vllm/model_executor/layers/quantization/utils/quant_utils.py`

### Summary

**What changed and why**  
Added support for asymmetric WNA16 quantization in MoE layers for the compressed-tensors method. Previously only symmetric quantization was allowed; this PR removes that constraint by handling zero points (asymmetric quantization) in weight processing, shape definitions, and Marlin kernel integration. The change improves model accuracy (lower perplexity) as shown in test results.

**Technical impact**  
Enables asymmetric quantization (e.g., W4A16_ASYM) for MoE models via compressed-tensors, which can improve model quality. Introduces new quant keys (kInt4StaticAsym, kInt4Static32Asym) and a new utility function (moe_packed_to_marlin_zero_points) to convert packed zero points to Marlin format. The weight shape logic and weight creation now handle zero point tensors. The Marlin MoE backend is extended to support asymmetric schemes.

**Potential risks**  
Asymmetric quantization may increase memory usage due to storing zero points. The new zero point conversion logic assumes compressed-tensors packing format; any changes to that format could break compatibility. The change only affects the Marlin backend; Flashinfer backend is not updated for asymmetric support. There is a risk of regression if asymmetric zero points are not correctly handled in all code paths (e.g., fused MoE kernel).

**Key insights**  
The PR resolves a limitation where asymmetric quantization was unsupported for MoEs, enabling better accuracy. The implementation reuses existing Marlin permutation functions for zero points. The test results show a clear perplexity improvement (word_perplexity from 9.3711 to 9.2505). The change is focused on the compressed-tensors quant method and does not affect other quantization schemes.

---

## 36. [[Misc] Support local image encoding in benchmarks](https://github.com/vllm-project/vllm/pull/43843)


### Base Information

- **PR Number:** #43843
- **Author:** [xiaozcy](https://github.com/xiaozcy)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-06-02 08:15:07
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/43843/files) (3):**
  - `docs/benchmarking/cli.md`
  - `tests/benchmarks/test_custom_image_dataset.py`
  - `vllm/benchmarks/datasets/datasets.py`

### Summary

**What changed and why**  
Added a `--custom-ensure-client-side-data` flag to the custom_image benchmark dataset that encodes local image files and HTTP(S) image URLs as base64 data URLs on the client side before sending requests. This addresses the issue where the serving endpoint cannot access local image paths when the benchmark client and server run on different machines or containers.

**Technical impact**  
Introduces a new CLI option and modifies the image processing pipeline in `process_image`, `_process_content_part`, `_process_interleaved_content`, `_process_image_files`, and `sample` methods to pass the `ensure_client_side_data` flag. The change also adds a dependency on `fetch_image` and `encode_image_url` from `vllm.multimodal.utils`. Tests are added to verify encoding of local files, remote URLs, and interleaved content, as well as rejection of invalid images.

**Potential risks**  
The new flag changes the default behavior of image handling when enabled, which could affect existing benchmark setups if users inadvertently enable it. The encoding process may increase memory usage and latency for large images. The `fetch_image` function may raise exceptions for unreachable URLs or corrupt files, which are caught and re-raised as `ValueError`. The change also includes a minor fix to handle `tokenizer=None` in prompt length calculation.

**Key insights**  
The PR enables client-side image encoding for benchmarks, making them more portable across environments. The implementation reuses existing `fetch_image` and `encode_image_url` utilities, ensuring consistency with other parts of vLLM. The test coverage is thorough, including edge cases like spaces in filenames and interleaved content with detail parameters.

---

## 37. [Migrate header files to torch stable abi](https://github.com/vllm-project/vllm/pull/44013)


### Base Information

- **PR Number:** #44013
- **Author:** [cleonard530](https://github.com/cleonard530)
- **Merged By:** [simon-mo](https://github.com/simon-mo)
- **Merged time:** 2026-06-02 08:09:52
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44013/files) (18):**
  - `.pre-commit-config.yaml`
  - `csrc/libtorch_stable/async_util.cuh`
  - `csrc/libtorch_stable/cutlass_extensions/epilogue/broadcast_load_epilogue_c2x.hpp`
  - `csrc/libtorch_stable/cutlass_extensions/epilogue/scaled_mm_epilogues_c2x.hpp`
  - `csrc/libtorch_stable/fused_qknorm_rope_kernel.cu`
  - `csrc/libtorch_stable/launch_bounds_utils.h`
  - `csrc/libtorch_stable/persistent_topk.cuh`
  - `csrc/libtorch_stable/quantization/fp4/activation_nvfp4_quant_fusion_kernels.cu`
  - `csrc/libtorch_stable/quantization/fp4/mxfp4_experts_quant.cu`
  - `csrc/libtorch_stable/quantization/fp4/nvfp4_experts_quant.cu`
  - `csrc/libtorch_stable/quantization/fp4/nvfp4_quant_kernels.cu`
  - `csrc/libtorch_stable/quantization/gguf/dequantize.cuh`
  - `csrc/libtorch_stable/quantization/gguf/ggml-common.h`
  - `csrc/libtorch_stable/quantization/gguf/gguf_kernel.cu`
  - `csrc/libtorch_stable/quantization/gguf/mmq.cuh`
  - `csrc/libtorch_stable/quantization/gguf/mmvq.cuh`
  - `csrc/libtorch_stable/quantization/gguf/vecdotq.cuh`
  - `csrc/libtorch_stable/topk.cu`

### Summary

**What changed and why**  
Moved header files used only by stable kernels into the libtorch_stable directory to support the migration to libtorch stable ABI. Updated include paths in several source files to reflect the new locations, and adjusted the clang-format exclude pattern in .pre-commit-config.yaml to match the new paths.

**Technical impact**  
This change reorganizes header files without altering functionality. It improves code organization by colocating stable headers with their users, and ensures that the clang-format hook correctly excludes the moved files. The include path updates are straightforward and maintain the same dependencies.

**Potential risks**  
Low risk. The changes are mechanical renames and include path adjustments. However, any external code that directly includes these headers using the old paths would break. Since these headers are internal to the project, the risk is minimal. The test results confirm no regressions.

**Key insights**  
The PR is part of a larger effort to isolate stable ABI code. The decision to move headers rather than leave them in place simplifies future maintenance and makes the stable ABI boundary clearer. The careful handling of git blame history (by not reformatting) shows attention to developer workflow.

---

## 38. [[Rust Frontend] Cover different thinking modes in roundtrip tests](https://github.com/vllm-project/vllm/pull/44320)


### Base Information

- **PR Number:** #44320
- **Author:** [BugenZhao](https://github.com/BugenZhao)
- **Merged By:** [njhill](https://github.com/njhill)
- **Merged time:** 2026-06-02 07:51:26
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44320/files) (1):**
  - `rust/src/chat/tests/roundtrip.rs`

### Summary

**What changed and why**  
Added a `ThinkingBehavior` enum to model different thinking modes (toggleable vs always, with defaults) and updated roundtrip tests to iterate over all relevant thinking configurations per model. This ensures the reasoning parser is tested with explicit enable, explicit disable, and default behavior, covering all initial parser states.

**Technical impact**  
The roundtrip test for reasoning and content now runs multiple sub-tests per model case, increasing test coverage. The `roundtrip_request` helper now accepts an optional `thinking` parameter to control template kwargs. The `run_roundtrip_reasoning_and_content` function loops over fixtures and calls a new inner function. The `RoundtripCase` struct gains a `thinking_behavior` field, and all model cases are updated accordingly.

**Potential risks**  
The change is limited to test code; no production logic is affected. The main risk is that the new test loops may increase test execution time, but this is acceptable for improved coverage. Incorrect fixture definitions could lead to false positives or negatives, but the existing test infrastructure should catch mismatches.

**Key insights**  
The PR systematically addresses a gap in test coverage by parameterizing thinking behavior. The `ThinkingBehavior` enum cleanly abstracts model-specific template behavior. The use of `fixtures()` method to generate test cases is a good pattern for exhaustive testing. The change is well-structured and follows existing code conventions.

---

## 39. [[ROCm] Fix AITER RMSNormQuantFusion for Kimi-Linear](https://github.com/vllm-project/vllm/pull/44308)


### Base Information

- **PR Number:** #44308
- **Author:** [pschlan-amd](https://github.com/pschlan-amd)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-06-02 07:50:21
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44308/files) (1):**
  - `vllm/compilation/passes/fusion/rocm_aiter_fusion.py`

### Summary

**What changed and why**  
Fixed a crash when using AITER with Kimi-Linear models by replacing direct attribute access `layer.num_v_heads` and `layer.head_v_dim` with fallback to `layer.num_heads` and `layer.head_dim` respectively, since `KimiGatedDeltaNetAttention` uses different attribute names.

**Technical impact**  
Enables AITER RMSNormQuantFusion for Kimi-Linear models, allowing them to run with vLLM and AITER support without crashing. The change is backward-compatible as it uses `getattr` with fallback.

**Potential risks**  
Low risk; the change only adds fallback logic for attribute access. The assertion ensures both attributes are present, so any missing attribute will raise an error early.

**Key insights**  
The fix addresses a model-specific attribute naming inconsistency. Using `getattr` with fallback is a robust pattern for handling different attention implementations.

---

## 40. [[Rust Frontend] Support recursive tool parameter conversion](https://github.com/vllm-project/vllm/pull/44299)


### Base Information

- **PR Number:** #44299
- **Author:** [BugenZhao](https://github.com/BugenZhao)
- **Merged By:** [njhill](https://github.com/njhill)
- **Merged time:** 2026-06-02 07:45:35
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44299/files) (2):**
  - `rust/src/tool-parser/src/deepseek_dsml/mod.rs`
  - `rust/src/tool-parser/src/parameters.rs`

### Summary

**What changed and why**  
Introduced a `ParamInput` enum to represent both raw text and structured parameter inputs, and made schema handling recursive for nested object properties, additional properties, and array items. This refactoring prepares for future parser-produced structured inputs while preserving existing string-based behavior.

**Technical impact**  
The `convert_params_with_schema` and `convert_param_with_schema` methods now accept generic `Into<ParamInput>` parameters, enabling callers to pass either strings or structured elements. The `JsonParamType` variants `Object` and `Array` now carry child schema information, allowing recursive conversion of nested values. The codebase gains 435 lines of new logic for recursive conversion, including handling of duplicate element names and typed additional properties.

**Potential risks**  
The recursive conversion logic may introduce edge cases for deeply nested or malformed schemas. The change to `JsonParamType` variants could affect existing pattern matching outside the module (though it's `pub(super)`). The fallback behavior for unknown parameters now returns object-like JSON for structured inputs instead of always a string, which might break callers expecting only strings.

**Key insights**  
The PR is a well-structured refactoring that adds recursive schema-aware conversion without altering existing parser behavior. The use of `Into<ParamInput>` provides a clean migration path. The addition of `ParamInput::Elements` and recursive handling of `properties`, `additionalProperties`, and `items` makes the system extensible for future structured parsers.

---

## 41. [[XPU] [Bug] remove xpuw4a16 output size check](https://github.com/vllm-project/vllm/pull/44168)


### Base Information

- **PR Number:** #44168
- **Author:** [zufangzhu](https://github.com/zufangzhu)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-06-02 07:26:20
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44168/files) (1):**
  - `vllm/model_executor/kernels/linear/mixed_precision/xpu.py`

### Summary

**What changed and why**  
Removed a check that required the output size (partition_weight_shape[1]) to be a multiple of 32 for XPUw4a16 kernel. The check was likely too restrictive or incorrect, and the kernel may actually support arbitrary output sizes.

**Technical impact**  
The XPUw4a16 kernel can now be used with output sizes that are not multiples of 32, which increases the range of supported model configurations on XPU devices. This may enable models that were previously blocked by this constraint.

**Potential risks**  
If the kernel truly does not support non-multiple-of-32 output sizes, removing the check could lead to runtime errors or incorrect results. The change assumes the check was unnecessary, but without testing or documentation, there is a risk of regression.

**Key insights**  
The removed check was for the output dimension, not the input dimension (which still has a multiple-of-32 check). This suggests the kernel's weight layout may only constrain the input size, not the output size. The change is small but could have significant impact on model compatibility.

---

## 42. [[BugFix] [GDN] Read linear_key_head_dim from hf_text_config for multimodal models](https://github.com/vllm-project/vllm/pull/43978)


### Base Information

- **PR Number:** #43978
- **Author:** [IdoAtadTD](https://github.com/IdoAtadTD)
- **Merged By:** [tomeras91](https://github.com/tomeras91)
- **Merged time:** 2026-06-02 07:17:55
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/43978/files) (1):**
  - `vllm/model_executor/layers/mamba/gdn/qwen_gdn_linear_attn.py`

### Summary

**What changed and why**  
Changed two references from `hf_config` to `hf_text_config` when reading `linear_key_head_dim` for multimodal Qwen3.5 models. The attribute is stored on `hf_text_config` for multimodal configs, so the previous code returned `None`, preventing CuteDSL/FlashInfer GDN prefill backends from being enabled on Blackwell GPUs.

**Technical impact**  
Fixes a bug where multimodal Qwen3.5 models (e.g., Qwen3.5-397B-A17B) would never use CuteDSL or FlashInfer GDN prefill kernels on SM100, falling back to Triton/FLA. Now `head_k_dim` is correctly read as 128, enabling the optimized backends when available.

**Potential risks**  
Low risk. The change is a two-line attribute lookup fallback that mirrors existing patterns in the codebase. Non-multimodal models are unaffected because `hf_text_config` falls back to `hf_config` in vLLM's model config. Regression is unlikely.

**Key insights**  
Multimodal models in vLLM may store text-specific config in `hf_text_config` rather than `hf_config`. This fix aligns GDN backend selection with other MLA backends that already use `hf_text_config`. The bug was subtle because it only affected multimodal variants and only manifested as a silent fallback to slower kernels.

---

## 43. [[FlashAttention] Sync FA with upstream](https://github.com/vllm-project/vllm/pull/44065)


### Base Information

- **PR Number:** #44065
- **Author:** [MatthewBonanni](https://github.com/MatthewBonanni)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-06-02 07:15:38
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44065/files) (1):**
  - `cmake/external_projects/vllm_flash_attn.cmake`

### Summary

**What changed and why**  
Updated the GIT_TAG for the vllm-flash-attn dependency from bce29425653ec0fbc579d329883030e832d15ada to dd62dac706b1cf7895bd99b18c6cb7e7e117ee25 to sync with upstream flash-attention changes. This is a dependency update that pulls in new commits from the upstream repository.

**Technical impact**  
Changes the pinned version of the flash-attention library used in the build. This may introduce new features, bug fixes, or performance improvements from upstream, but could also alter behavior or introduce regressions. The build system will fetch and compile the new version.

**Potential risks**  
The new version may contain breaking changes or incompatibilities with the current codebase. There is a risk of build failures or runtime errors if the new tag introduces API changes or behavioral differences. Testing is required to ensure compatibility.

**Key insights**  
This is a straightforward dependency version bump to align with upstream. The change is minimal (only two lines modified) but has significant implications as it updates a core library. The PR description references a corresponding upstream PR, indicating a deliberate sync effort.

---

## 44. [[Bugfix] Vendor MiniCPMV/MiniCPMO processors to unblock Transformers v5](https://github.com/vllm-project/vllm/pull/44282)


### Base Information

- **PR Number:** #44282
- **Author:** [wjinxu](https://github.com/wjinxu)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-06-02 07:14:39
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44282/files) (7):**
  - `tests/lora/test_minicpmv_tp.py`
  - `tests/models/multimodal/generation/test_common.py`
  - `vllm/model_executor/models/minicpmo.py`
  - `vllm/model_executor/models/minicpmv.py`
  - `vllm/transformers_utils/processors/__init__.py`
  - `vllm/transformers_utils/processors/minicpmo.py`
  - `vllm/transformers_utils/processors/minicpmv.py`

### Summary

**What changed and why**  
Vendored MiniCPMVProcessor and MiniCPMOProcessor into vLLM to bypass Transformers v5 incompatibility where custom processors rely on tokenizer attributes not available in the new TokenizersBackend. This unblocks the Transformers v5 upgrade and fixes related test skips.

**Technical impact**  
Adds two new processor files (minicpmv.py, minicpmo.py) and modifies model files to use vendored processors instead of HF ones. Removes skip markers from tests, enabling previously skipped tests. The change is backward-compatible as it preserves existing behavior while adding v5 compatibility.

**Potential risks**  
Vendored processors may diverge from upstream HF implementations over time, requiring maintenance. The manual merge conflict resolution in minicpmv.py could introduce subtle bugs if not thoroughly tested. Audio processing in MiniCPMOProcessor is complex and may have edge cases.

**Key insights**  
The PR addresses a critical blocker for Transformers v5 by vendoring processors that rely on removed tokenizer attributes. The approach follows a pattern used for other models (e.g., Kimi, Pixtral). The commit history is preserved via cherry-pick, maintaining attribution. Outstanding review comments are deferred to follow-ups.

---

## 45. [Support ModelOpt MXFP8 non-gated MoE](https://github.com/vllm-project/vllm/pull/42958)


### Base Information

- **PR Number:** #42958
- **Author:** [TomerBN-Nvidia](https://github.com/TomerBN-Nvidia)
- **Merged By:** [tomeras91](https://github.com/tomeras91)
- **Merged time:** 2026-06-02 06:56:04
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/42958/files) (1):**
  - `vllm/model_executor/layers/fused_moe/experts/trtllm_fp8_moe.py`

### Summary

**What changed and why**  
Extended the TRTLLM MXFP8 MoE kernel to support non-gated ReLU2 activation (RELU2_NO_MUL) in addition to SiLU. This enables ModelOpt MXFP8 TRTLLM MoE to run non-gated activations through the FlashInfer backend. The change also refactors the call to pass activation_type when needed and adjusts n_group/topk_group handling for MXFP8 vs DeepSeekFp8 cases.

**Technical impact**  
Adds support for a new activation function in the MXFP8 MoE kernel, expanding the range of models that can use this optimized path. The refactoring improves code clarity and ensures correct parameter passing for different quantization types. The removal of the topk <= 10 assertion for MXFP8 may allow larger topk values in that path.

**Potential risks**  
The new activation_type parameter is only passed for MXFP8 or RELU2_NO_MUL; other cases may silently use default behavior. The change in n_group/topk_group handling (None vs 0) could affect kernel behavior if not handled correctly by FlashInfer. The removal of the topk <= 10 assertion for MXFP8 might expose unsupported configurations.

**Key insights**  
The change leverages FlashInfer's existing support for different activation types in MXFP8 MoE kernels. The refactoring separates MXFP8 and DeepSeekFp8 code paths more clearly, reducing conditional complexity. The addition of activation_type parameter is conditional, indicating that FlashInfer's default activation might be SiLU.

---

## 46. [[Bugfix] Fix Gemma4 startup crash with recent transformers multimodal processor](https://github.com/vllm-project/vllm/pull/44232)


### Base Information

- **PR Number:** #44232
- **Author:** [lucianommartins](https://github.com/lucianommartins)
- **Merged By:** [Isotr0py](https://github.com/Isotr0py)
- **Merged time:** 2026-06-02 06:42:41
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44232/files) (1):**
  - `vllm/model_executor/models/gemma4_mm.py`

### Summary

**What changed and why**  
Added an override of `_apply_hf_processor_text_only` in `Gemma4MultiModalProcessor` to bypass the HF processor's multimodal expansion pipeline when tokenizing text-only prompts. This fixes a `StopIteration` crash caused by recent `transformers` changes that enforce 1:1 matching between placeholder tokens and replacement data.

**Technical impact**  
Prevents server startup failure during KV cache profiling when using recent `transformers` versions. The fix ensures that text-only tokenization works correctly for prompts containing multimodal placeholders without corresponding data, preserving placeholder token IDs for downstream expansion.

**Potential risks**  
Low risk. The change is localized to the text-only tokenization path and does not affect multimodal processing. It relies on the tokenizer directly, which is a standard operation. Regression is unlikely as the override only applies to the specific scenario that was failing.

**Key insights**  
The root cause is a behavioral change in `transformers`' `ProcessorMixin.__call__` that now raises `StopIteration` when multimodal placeholders lack matching data. The fix bypasses the processor for text-only calls, which is safe because the tokenizer alone suffices for generating token IDs. This pattern could be applicable to other models with similar multimodal processors.

---

## 47. [[Bugfix] Sync block_size from EngineCore to frontend for hybrid Mamba…](https://github.com/vllm-project/vllm/pull/42967)


### Base Information

- **PR Number:** #42967
- **Author:** [Gruner-atero](https://github.com/Gruner-atero)
- **Merged By:** [ZJY0516](https://github.com/ZJY0516)
- **Merged time:** 2026-06-02 06:41:00
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/42967/files) (4):**
  - `tests/v1/engine/test_engine_core_client.py`
  - `vllm/v1/engine/__init__.py`
  - `vllm/v1/engine/core.py`
  - `vllm/v1/engine/core_client.py`

### Summary

**What changed and why**  
Added block_size field to EngineCoreReadyResponse and synced it from worker to frontend to fix stale block_size reporting for hybrid Mamba models. Previously, _align_hybrid_block_size() enlarged block_size in the worker but the update was never communicated back, causing cache_config_info to show the default value.

**Technical impact**  
Ensures the frontend APIServer uses the actual runtime block_size (e.g., 528 or 1056) instead of the default 16 for hybrid Mamba models. This fixes incorrect cache configuration reporting and ensures consistency between worker and frontend.

**Potential risks**  
Low risk. The change is minimal and follows existing patterns for syncing num_gpu_blocks. The use of max() in DP multi-engine case is not explicitly shown but implied; if block_size differs across engines, the last response overwrites previous values, which could be inconsistent. However, block_size is expected to be uniform across engines.

**Key insights**  
The bug was caused by a missing synchronization path for block_size after model-specific alignment. The fix adds block_size to the ready response and applies it in _apply_ready_response, mirroring the existing num_gpu_blocks sync. A unit test validates the new behavior.

---

## 48. [[Frontend] Consolidate dev entrypoints.](https://github.com/vllm-project/vllm/pull/44170)


### Base Information

- **PR Number:** #44170
- **Author:** [noooop](https://github.com/noooop)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-06-02 06:30:22
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44170/files) (22):**
  - `.buildkite/test-amd.yaml`
  - `.buildkite/test_areas/entrypoints.yaml`
  - `.buildkite/test_areas/rust_frontend.yaml`
  - `docs/serving/online_serving/README.md`
  - `tests/entrypoints/serve/dev/__init__.py`
  - `tests/entrypoints/serve/dev/rpc/__init__.py`
  - `tests/entrypoints/serve/dev/rpc/test_collective_rpc.py`
  - `tests/entrypoints/serve/dev/test_sleep.py`
  - `vllm/entrypoints/openai/api_server.py`
  - `vllm/entrypoints/serve/__init__.py`
  - `vllm/entrypoints/serve/dev/__init__.py`
  - `vllm/entrypoints/serve/dev/cache/__init__.py`
  - `vllm/entrypoints/serve/dev/cache/api_router.py`
  - `vllm/entrypoints/serve/dev/rlhf/__init__.py`
  - `vllm/entrypoints/serve/dev/rlhf/api_router.py`
  - `vllm/entrypoints/serve/dev/rpc/__init__.py`
  - `vllm/entrypoints/serve/dev/rpc/api_router.py`
  - `vllm/entrypoints/serve/dev/server_info/__init__.py`
  - `vllm/entrypoints/serve/dev/server_info/api_router.py`
  - `vllm/entrypoints/serve/dev/sleep/__init__.py`
  - `vllm/entrypoints/serve/dev/sleep/api_router.py`
  - `vllm/entrypoints/serve/instrumentator/__init__.py`

### Summary

**What changed and why**  
Consolidated all development-mode entrypoints into vllm/entrypoints/serve/dev/ directory, moving cache, rlhf, rpc, server_info, and sleep routers from their previous locations. Updated the documentation in docs/serving/online_serving/README.md to reflect the new structure and added security warnings. Adjusted CI pipeline configurations (test-amd.yaml, test_areas/entrypoints.yaml, rust_frontend.yaml) to update test paths and dependencies accordingly. Removed the VLLM_SERVER_DEV_MODE condition checks from individual router modules since the registration is now gated at the app level.

**Technical impact**  
This refactor centralizes all dev-only endpoints under a single directory, making it easier to manage and disable them in production. The registration logic is moved from individual routers to a new function register_vllm_dev_api_routers in vllm/entrypoints/serve/__init__.py, which is only called when VLLM_SERVER_DEV_MODE is set. This reduces code duplication and ensures consistent security warnings. CI test commands are updated to reflect the new test file locations, and some test dependencies are broadened (e.g., from specific subdirectories to the whole serve directory) to improve coverage.

**Potential risks**  
The change moves several router files and updates import paths, which could break if any external code references the old paths. The CI changes modify test commands and source file dependencies, which might cause tests to be skipped or run incorrectly if the new paths are not correctly set. The removal of VLLM_SERVER_DEV_MODE checks from individual routers means that if register_vllm_dev_api_routers is accidentally called without the env var, dev endpoints could be exposed. However, the current code only calls it conditionally.

**Key insights**  
The PR consolidates dev entrypoints into a single directory, improving maintainability and security. The documentation is updated to clearly list all dev endpoints with security warnings. The CI changes reflect the new test structure and also add missing tool_use tests to the openai part 1 test step. The refactor removes redundant env var checks from individual routers, centralizing the gating logic.

---

## 49. [Fix DFlash prefix cache corruption due to missing lookahead block](https://github.com/vllm-project/vllm/pull/42971)


### Base Information

- **PR Number:** #42971
- **Author:** [shreyas269](https://github.com/shreyas269)
- **Merged By:** [benchislett](https://github.com/benchislett)
- **Merged time:** 2026-06-02 05:06:34
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/42971/files) (2):**
  - `tests/v1/spec_decode/test_dflash_lookahead.py`
  - `vllm/v1/worker/gpu_model_runner.py`

### Summary

**What changed and why**  
Fixed a KV cache corruption bug in DFlash where the drafter writes into shared prefix blocks because lookahead blocks were not allocated for new requests. The scheduler's guard (num_computed_tokens == 0) incorrectly prevented lookahead allocation at prefill time, but DFlash needs those blocks immediately. Also fixed the drafter input-fit check to account for DFlash's extra bonus token query.

**Technical impact**  
Prevents persistent MGL degradation under prefix caching with concurrency. Without prefix caching, the bug caused one bad step but recovery; with prefix caching, it corrupted shared blocks affecting all requests using that prefix. The fix ensures correct block allocation and drafter window sizing.

**Potential risks**  
Low risk. The scheduler fix was already merged separately. The remaining changes are well-tested with three unit tests covering block reservation, query window fitting, and bonus token handling. The input-fit check change is conservative (rejects sequences that were incorrectly admitted before).

**Key insights**  
The root cause was a subtle interaction between scheduler block allocation logic and DFlash's bidirectional draft attention. The num_computed_tokens guard was intended for P/D disaggregation but broke DFlash because num_computed_tokens is still 0 during first allocation. The fix ensures DFlash always gets lookahead blocks on first prefill.

---

## 50. [[XPU][Mamba] Triton-based selective scan forward op for XPU](https://github.com/vllm-project/vllm/pull/43421)


### Base Information

- **PR Number:** #43421
- **Author:** [mfylcek](https://github.com/mfylcek)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-06-02 03:50:26
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/43421/files) (2):**
  - `vllm/_xpu_ops.py`
  - `vllm/model_executor/layers/mamba/ops/mamba_ssm.py`

### Summary

**What changed and why**  
Added a Triton-based selective scan forward kernel for XPU devices to enable Mamba1 prefill. The kernel is implemented in vllm/_xpu_ops.py and the dispatch logic in mamba_ssm.py is updated to call the XPU-specific op when running on XPU.

**Technical impact**  
Introduces a new Triton kernel for selective scan on XPU, with support for variable-length sequences, caching, and optional features like delta bias and softplus. The kernel is integrated into the existing Mamba layer, enabling Mamba1 prefill on Intel XPU devices.

**Potential risks**  
The new kernel may have performance or correctness issues on XPU hardware, especially for edge cases like empty sequences or extreme values. The kernel is only used on XPU, so other platforms are unaffected. Testing is limited to a single model (falcon-mamba-7b) on GSM8K, which may not cover all scenarios.

**Key insights**  
The implementation mirrors the existing CUDA selective scan kernel but uses Triton for portability. The kernel handles complex features like APC (automatic prefix caching) and variable-length sequences. The code is well-structured with compile-time constants for optional features, which should help performance.

---

## 51. [[KV Offload] Add `on_schedule_end()` hook to separate step lifecycle from event draining](https://github.com/vllm-project/vllm/pull/44206)


### Base Information

- **PR Number:** #44206
- **Author:** [ronensc](https://github.com/ronensc)
- **Merged By:** [orozery](https://github.com/orozery)
- **Merged time:** 2026-06-02 03:42:52
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44206/files) (5):**
  - `tests/v1/kv_offload/test_tiering_offloading.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/offloading/scheduler.py`
  - `vllm/v1/kv_offload/base.py`
  - `vllm/v1/kv_offload/tiering/base.py`
  - `vllm/v1/kv_offload/tiering/manager.py`

### Summary

**What changed and why**  
Added `on_schedule_end()` lifecycle hook to `OffloadingManager` and `SecondaryTierManager`, called from `OffloadingConnectorScheduler.build_connector_meta()` at the end of each scheduler step. This separates step-lifecycle work (processing finished jobs, flushing batched promotions, resetting the per-step gate) from pure event observation in `take_events()`. The change improves separation of concerns and prepares for future refactoring.

**Technical impact**  
The `OffloadingManager.on_schedule_end()` method now handles deferred work that was previously done in `take_events()`, while `take_events()` only yields events. Tests are updated to call `on_schedule_end()` followed by `take_events()` instead of just `take_events()`. This ensures the step lifecycle is explicitly managed and reduces coupling between event draining and step processing.

**Potential risks**  
If any code path calls `take_events()` without calling `on_schedule_end()` first, deferred work (like flushing promotions) will not be executed, potentially causing stale state or missed promotions. However, the scheduler now calls `on_schedule_end()` before `take_events()`, so normal operation is safe. Tests that rely on the old behavior have been updated.

**Key insights**  
The PR introduces a clear lifecycle hook that separates concerns: `on_schedule_end()` for step-end processing and `take_events()` for event observation. This makes the code more maintainable and testable. The change is backward-compatible for external callers that only use `take_events()`, as long as they also call `on_schedule_end()` appropriately.

---

## 52. [[HARDWARE][POWER] Enable SHM communicator support for PowerPC](https://github.com/vllm-project/vllm/pull/43754)


### Base Information

- **PR Number:** #43754
- **Author:** [Rukhaiya2004](https://github.com/Rukhaiya2004)
- **Merged By:** [bigPYJ1151](https://github.com/bigPYJ1151)
- **Merged time:** 2026-06-02 03:06:33
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/43754/files) (6):**
  - `=4.5.1`
  - `cmake/cpu_extension.cmake`
  - `csrc/cpu/cpu_types_vsx.hpp`
  - `csrc/cpu/shm.cpp`
  - `csrc/cpu/torch_bindings.cpp`
  - `vllm/distributed/device_communicators/cpu_communicator.py`

### Summary

**What changed and why**  
This PR enables SHM (shared memory) communicator support for PowerPC architecture. It modifies CMake to include SHM source files for PowerPC, adds FP16Vec16 and INT8Vec64 vector types for VSX, adds conversion constructors between FP16Vec16 and FP32Vec16, extends atomic stamp usage to PowerPC in shm.cpp, adds PowerPC-specific spin-loop hint, and updates Python and Torch bindings to conditionally enable SHM for PowerPC. The change improves performance on PowerPC systems by leveraging shared memory communication.

**Technical impact**  
Adds SHM communicator support for PowerPC, which can improve throughput and latency for tensor-parallel inference on PowerPC systems. The changes are isolated to PowerPC-specific code paths and do not affect other architectures. Performance improvements of ~2-3% in token throughput and reduced TTFT/TPOT are reported.

**Potential risks**  
Risk is low as changes are guarded by architecture-specific preprocessor checks and only affect PowerPC builds. Potential issues include incorrect vector type conversions or atomic operations on older PowerPC CPUs that lack VSX or PowerISA 2.07+ features. The spin-loop hint 'or 1,1,1' is a no-op on some implementations, but should be safe.

**Key insights**  
The PR demonstrates that SHM communicator, previously limited to x86 and ARM, can be extended to PowerPC with minimal changes. The addition of FP16Vec16 and INT8Vec64 types suggests ongoing work to support mixed-precision and quantization on PowerPC. The use of atomic operations for memory ordering on PowerPC is appropriate due to its weak memory model.

---

## 53. [[Multimodal] Automatically select registered video loader for VLM](https://github.com/vllm-project/vllm/pull/44126)


### Base Information

- **PR Number:** #44126
- **Author:** [Isotr0py](https://github.com/Isotr0py)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-06-02 02:09:48
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44126/files) (5):**
  - `tests/multimodal/test_video.py`
  - `vllm/entrypoints/chat_utils.py`
  - `vllm/multimodal/media/connector.py`
  - `vllm/multimodal/video.py`
  - `vllm/transformers_utils/processor.py`

### Summary

**What changed and why**  
Enhanced video loader registration to automatically select the correct video loader backend based on the model's video processor type, improving user experience by removing the need to manually set VLLM_VIDEO_LOADER_BACKEND. Added a new VideoLoaderRegistry class that maps video processor class names to backend names, and updated the connector and chat utils to pass the video processor name when fetching videos.

**Technical impact**  
Introduces a new registry mechanism (VideoLoaderRegistry) that extends ExtensionManager with a processor2backend mapping. Adds two new functions (get_video_processor_cls_name_from_config and get_video_processor_cls_name) to retrieve the video processor type from model config. Modifies VideoMediaIO construction in connector.py to automatically set video_backend if not already specified. Updates chat_utils.py to pass video_processor_name to fetch_video methods. Renames GLM4_6VVideoBackend to GLMGAVideoBackend and updates its registration. Adds tests for the new resolution logic.

**Potential risks**  
The automatic backend selection may fail if the video processor type is not registered or if the config file lacks the video_processor_type field, potentially falling back to default behavior. The rename of GLM4_6VVideoBackend to GLMGAVideoBackend could break external code that references the old class name. The caching of get_video_processor_cls_name may cause stale results if model config changes dynamically. The new dependency on processor config files may increase startup time for models without video support.

**Key insights**  
The PR addresses a common usability pain point by automating video loader selection. The design ties the registry to the video processor class name, which is a clean abstraction. The addition of tests ensures the mapping works for specific models (Molmo2, GLM-4V). The rename of GLM4_6VVideoBackend suggests a correction or alignment with model naming. The changes are well-structured and follow existing patterns in the codebase.

---

## 54. [[Parser] Migrate `ResponsesParser` to unified `Parser` interface](https://github.com/vllm-project/vllm/pull/42977)


### Base Information

- **PR Number:** #42977
- **Author:** [albertoperdomo2](https://github.com/albertoperdomo2)
- **Merged By:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged time:** 2026-06-02 01:50:05
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/42977/files) (4):**
  - `tests/entrypoints/openai/test_responses_parser_unified.py`
  - `vllm/entrypoints/openai/parser/responses_parser.py`
  - `vllm/entrypoints/openai/responses/context.py`
  - `vllm/entrypoints/openai/responses/serving.py`

### Summary

**What changed and why**  
Migrated ResponsesParser from using separate ReasoningParser and ToolParser instances to a unified Parser interface, addressing a TODO from RFC #32713. This eliminates inconsistency between streaming and non-streaming code paths in the Responses API, and fixes tool_choice modes (forced, required) in ParsableContext which previously only handled auto.

**Technical impact**  
Replaces two constructor parameters (reasoning_parser_cls, tool_parser_cls) with a single parser_cls parameter. The process() method now delegates to Parser.extract_response_outputs() instead of calling parsers independently. Factory function and ParsableContext updated accordingly. Adds enable_auto_tools and tool_call_id_type parameters. Removes direct instantiation of reasoning/tool parsers. No behavior change for default SimpleContext path.

**Potential risks**  
The change is gated behind VLLM_USE_EXPERIMENTAL_PARSER_CONTEXT, so default behavior is unaffected. Risks include: (1) if the unified Parser interface does not fully replicate the old behavior for edge cases (e.g., custom reasoning/tool parsers), (2) the new tool_choice handling in ParsableContext may introduce regressions for non-auto modes, (3) the refactored factory function signature may break external code that directly instantiates ResponsesParser. Tests cover basic text, reasoning, and tool call scenarios but may not cover all edge cases.

**Key insights**  
The PR consolidates two parser paths into one, reducing code duplication and ensuring consistent behavior. The addition of enable_auto_tools and tool_call_id_type parameters suggests the unified parser now handles tool choice modes that were previously missing. The test file is large (+382 lines) indicating thorough unit test coverage. The change is part of a larger RFC-driven migration toward a unified parser interface.

---

## 55. [[EC Connector] Non blocking EC Connector lookup](https://github.com/vllm-project/vllm/pull/41627)


### Base Information

- **PR Number:** #41627
- **Author:** [omerpaz95](https://github.com/omerpaz95)
- **Merged By:** [orozery](https://github.com/orozery)
- **Merged time:** 2026-06-02 01:48:25
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/41627/files) (3):**
  - `tests/v1/core/test_scheduler.py`
  - `vllm/distributed/ec_transfer/ec_connector/base.py`
  - `vllm/v1/core/sched/scheduler.py`

### Summary

**What changed and why**  
Introduced a non-blocking deferral mechanism for EC connector lookups in the scheduler. Added `ensure_cache_available()` API to `ECConnectorBase` that the scheduler polls; if it returns False (prefetch in progress), the request is skipped and re-queued in `step_skipped_waiting`, allowing other requests to proceed. This prevents blocking the scheduler when multimodal encoder cache is still being staged.

**Technical impact**  
The scheduler now conditionally defers requests with pending encoder cache prefetches, improving throughput by not stalling on incomplete cache transfers. The change adds a new method to the base connector class (default returns True) and a check in the scheduling loop. Tests verify deferral behavior and correct filtering of future multimodal features.

**Potential risks**  
If `ensure_cache_available()` is not properly implemented by a connector, it may always return False, causing indefinite deferral. The deferral path could interact with other scheduling logic (e.g., preemption, prefix caching) in unforeseen ways. Performance impact of the additional check per request per step is minimal but should be monitored.

**Key insights**  
The design uses a polling approach rather than callbacks, keeping the scheduler non-blocking and simple. The filtering of future mm features ensures only relevant prefetches are considered. The test coverage includes both KV connector and non-KV connector modes, and validates that deferred requests are eventually scheduled when cache becomes available.

---

## 56. [[Core][Refactor]: thread `scheduler_block_size` into KVCacheManager and KVCacheCoordinator](https://github.com/vllm-project/vllm/pull/44165)


### Base Information

- **PR Number:** #44165
- **Author:** [ivanium](https://github.com/ivanium)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-06-02 01:14:45
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44165/files) (9):**
  - `tests/v1/core/test_kv_cache_utils.py`
  - `tests/v1/core/test_prefix_caching.py`
  - `tests/v1/core/test_single_type_kv_cache_manager.py`
  - `tests/v1/kv_connector/unit/offloading_connector/utils.py`
  - `vllm/v1/core/kv_cache_coordinator.py`
  - `vllm/v1/core/kv_cache_manager.py`
  - `vllm/v1/core/sched/scheduler.py`
  - `vllm/v1/core/single_type_kv_cache_manager.py`
  - `vllm/v1/simple_kv_offload/manager.py`

### Summary

**What changed and why**  
Threads an explicit `scheduler_block_size` parameter through `KVCacheManager`, `KVCacheCoordinator`, and `SingleTypeKVCacheManager`, replacing the duplicate LCM computation in `HybridKVCacheCoordinator`. This is a behavior-preserving refactor to prepare for a follow-up PR that needs the scheduling block size available at the manager/coordinator level.

**Technical impact**  
Adds a new required parameter `scheduler_block_size` to multiple classes and their constructors, updating all call sites in the scheduler, simple_kv_offload, and tests. The parameter is validated to be a multiple of `hash_block_size` and each group's block size. The `HybridKVCacheCoordinator` no longer computes `lcm_block_size` internally, relying on the passed-in value instead.

**Potential risks**  
Low risk since the change is behavior-preserving and the new parameter value is derived from the same LCM computation that was previously done internally. However, any external code that directly instantiates these classes (e.g., in tests or plugins) must be updated to provide the new parameter. The validation assertion could fail if the scheduler_block_size is incorrectly computed, but this would be caught early.

**Key insights**  
This refactor eliminates duplicated logic and makes the alignment invariant explicit, simplifying future changes that depend on the scheduling block size. The test helper `make_kv_cache_manager` reduces boilerplate by deriving the parameter automatically. The change is a clean example of preparatory refactoring to enable a larger feature.

---

## 57. [[Rust Frontend] add  --enable-request-id-headers flag support.](https://github.com/vllm-project/vllm/pull/43883)


### Base Information

- **PR Number:** #43883
- **Author:** [cinnamonica02](https://github.com/cinnamonica02)
- **Merged By:** [BugenZhao](https://github.com/BugenZhao)
- **Merged time:** 2026-06-02 01:08:38
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/43883/files) (11):**
  - `rust/src/cmd/src/cli.rs`
  - `rust/src/cmd/src/cli/tests.rs`
  - `rust/src/cmd/src/cli/unsupported.rs`
  - `rust/src/server/examples/external_engine_openai_qwen.rs`
  - `rust/src/server/src/config.rs`
  - `rust/src/server/src/lib.rs`
  - `rust/src/server/src/middleware/mod.rs`
  - `rust/src/server/src/middleware/request_id.rs`
  - `rust/src/server/src/routes.rs`
  - `rust/src/server/src/routes/tests.rs`
  - `rust/src/server/src/state.rs`

### Summary

**What changed and why**  
Migrated --enable-request-id-headers from unsupported args to a supported server option in the Rust frontend, adding a new middleware that appends X-Request-Id to HTTP responses. This matches Python vLLM's XRequestIdMiddleware behavior.

**Technical impact**  
Adds a new CLI flag and configuration field, a new middleware module, and conditional middleware registration in the router. Includes unit tests for CLI parsing and middleware behavior. The change is backward compatible as the feature defaults to disabled.

**Potential risks**  
Low risk. The feature is disabled by default and only adds a header to responses when enabled. No existing behavior is altered. The middleware is simple and well-tested.

**Key insights**  
The implementation correctly echoes incoming X-Request-Id or generates a UUID4 hex string. The conditional middleware registration avoids overhead when disabled. Tests cover disabled, generated, and echoed scenarios.

---

## 58. [[kv_offload] Add `@override` decorators to subclass method implementations](https://github.com/vllm-project/vllm/pull/44177)


### Base Information

- **PR Number:** #44177
- **Author:** [ronensc](https://github.com/ronensc)
- **Merged By:** [orozery](https://github.com/orozery)
- **Merged time:** 2026-06-02 01:07:47
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44177/files) (10):**
  - `vllm/v1/kv_offload/base.py`
  - `vllm/v1/kv_offload/cpu/common.py`
  - `vllm/v1/kv_offload/cpu/gpu_worker.py`
  - `vllm/v1/kv_offload/cpu/manager.py`
  - `vllm/v1/kv_offload/cpu/policies/arc.py`
  - `vllm/v1/kv_offload/cpu/policies/lru.py`
  - `vllm/v1/kv_offload/cpu/spec.py`
  - `vllm/v1/kv_offload/tiering/example/manager.py`
  - `vllm/v1/kv_offload/tiering/fs/manager.py`
  - `vllm/v1/kv_offload/tiering/manager.py`

### Summary

**What changed and why**  
Added `@override` decorators from `typing_extensions` to all method overrides in the `vllm/v1/kv_offload` package. This ensures that if a base class method is renamed or removed, static type checkers will flag the inconsistency, preventing silent breakage as the subsystem grows.

**Technical impact**  
No runtime behavior change; purely a static-typing annotation. The `@override` decorator is a no-op at runtime but enables mypy/pyright to catch mismatched overrides. This improves code maintainability and reduces the risk of subtle bugs during refactoring.

**Potential risks**  
Low risk. The decorator is a no-op at runtime, so no behavioral changes. However, if any method is incorrectly annotated (e.g., not actually overriding a base class method), static checkers will report an error, which may require fixing. This is a positive outcome.

**Key insights**  
The PR adds 68 lines of `@override` annotations across 10 files, covering all overridden methods in the kv_offload module. This is a proactive measure to enforce interface contracts statically, aligning with best practices for large codebases. The use of `typing_extensions.override` ensures compatibility with Python 3.10+.

---

## 59. [[CPU][Perf] Enable fused kernels for GDN's gated delta rules](https://github.com/vllm-project/vllm/pull/43534)


### Base Information

- **PR Number:** #43534
- **Author:** [fadara01](https://github.com/fadara01)
- **Merged By:** [bigPYJ1151](https://github.com/bigPYJ1151)
- **Merged time:** 2026-06-02 01:00:49
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/43534/files) (11):**
  - `.buildkite/hardware_tests/cpu.yaml`
  - `.buildkite/scripts/hardware_ci/run-cpu-test-arm.sh`
  - `cmake/cpu_extension.cmake`
  - `csrc/cpu/sgl-kernels/blas_gemm.h`
  - `csrc/cpu/sgl-kernels/fla.cpp`
  - `csrc/cpu/sgl-kernels/gemm.h`
  - `csrc/cpu/sgl-kernels/vec.h`
  - `csrc/cpu/torch_bindings.cpp`
  - `tests/kernels/mamba/cpu/test_cpu_gdn_ops.py`
  - `vllm/model_executor/layers/mamba/ops/cpu/gdn_attention.py`
  - `vllm/model_executor/layers/mamba/ops/cpu/recurrent_gated_delta_rule.py`

### Summary

**What changed and why**  
This PR enables fused kernels for GDN's gated delta rules on CPU, making them ISA-agnostic. It unifies AMX and non-AMX implementations, adds OpenBLAS support for non-x86 ISAs, fixes a beta pointer access bug in fused_sigmoid_gating_delta_rule_update, adds comprehensive tests, and removes the old Python-based recurrent_gated_delta_rule module in favor of the fused C++ kernels.

**Technical impact**  
The change significantly improves performance (50% higher throughput on Neoverse-V2) and broadens CPU support by making the kernels work on non-x86 architectures via OpenBLAS or PyTorch BLAS fallbacks. It also restructures the build system to conditionally compile fla.cpp for all ISAs and adds a new test file for GDN ops.

**Potential risks**  
The removal of the Python fallback (recurrent_gated_delta_rule.py) means any edge cases not covered by the fused kernels could break. The OpenBLAS detection relies on PyTorch wheel layout, which may vary across installations. The conditional compilation changes could introduce build errors on unsupported platforms if the macros are not correctly defined.

**Key insights**  
The PR demonstrates a clear strategy to optimize performance-critical kernels while maintaining portability by using compile-time ISA detection and fallback to generic BLAS. The fix for the beta pointer access in sigmoid calculation highlights the importance of careful memory management in fused kernels. The addition of tests ensures correctness across various sequence lengths and head configurations.

---

## 60. [[Perf] use triton moe backend on hopper by default](https://github.com/vllm-project/vllm/pull/44220)


### Base Information

- **PR Number:** #44220
- **Author:** [ZJY0516](https://github.com/ZJY0516)
- **Merged By:** [ZJY0516](https://github.com/ZJY0516)
- **Merged time:** 2026-06-02 00:52:30
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44220/files) (1):**
  - `vllm/model_executor/layers/fused_moe/oracle/unquantized.py`

### Summary

**What changed and why**  
Changed the default MoE backend preference on Hopper (SM90) GPUs to prioritize Triton over FlashInfer backends, because benchmarks show Triton achieves higher throughput (50.19 vs 48.35 req/s) on H200 with Qwen3-30B-A3B.

**Technical impact**  
On Hopper GPUs, the Triton MoE backend will be selected by default instead of FlashInfer, improving throughput by ~4% for MoE models. Non-Hopper GPUs are unaffected.

**Potential risks**  
Low risk; the change only reorders backend priority on SM90 and does not alter kernel logic. If Triton has undiscovered issues on Hopper, users can still override via --moe-backend.

**Key insights**  
The performance difference is small but consistent, and the change is safe as it only affects default selection. The PR references a benchmark issue for further details.

---

## 61. [[Refactor] Unify reasoning + tool-call parsing behind Parser.parse()](https://github.com/vllm-project/vllm/pull/44267)


### Base Information

- **PR Number:** #44267
- **Author:** [sfeng33](https://github.com/sfeng33)
- **Merged By:** [sfeng33](https://github.com/sfeng33)
- **Merged time:** 2026-06-02 00:11:43
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44267/files) (5):**
  - `tests/entrypoints/openai/test_tool_choice_content_none.py`
  - `tests/parser/test_parse.py`
  - `vllm/entrypoints/openai/chat_completion/serving.py`
  - `vllm/entrypoints/openai/engine/serving.py`
  - `vllm/parser/abstract_parser.py`

### Summary

**What changed and why**  
Consolidated reasoning extraction and tool-call parsing into a single Parser.parse() method. Previously, chat_completion_full_generator called reasoning_parser.extract_reasoning() and then OpenAIServing._parse_tool_calls_from_content() separately. Now DelegatingParser.parse() runs both steps sequentially, and the old static method was removed from serving.py and its logic moved into DelegatingParser._extract_tool_calls(). This reduces duplication and centralizes parsing logic.

**Technical impact**  
The non-streaming chat completions path now uses a unified parser object built in _create_chat_completion and passed to chat_completion_full_generator. The old static method _parse_tool_calls_from_content is deleted; its logic is replicated in _extract_tool_calls on DelegatingParser. The Parser abstract base class gains a new abstract method parse(). Tests are updated to use the new API, and a new test file tests/parser/test_parse.py covers various combinations of reasoning and tool parsing.

**Potential risks**  
The refactoring moves tool-call parsing logic from a static method to an instance method on DelegatingParser, which may affect subclasses that override extract_tool_calls or parse. The logic for named/required tool choices and Mistral tool parser is preserved but could have subtle differences due to the new code structure. The change only affects non-streaming path; streaming path is unchanged. If any custom parser subclasses exist outside the repo, they may need to implement the new parse() method.

**Key insights**  
The PR simplifies the call site in chat_completion_full_generator by replacing two separate calls with one parser.parse() call. The old static method had complex branching for different tool_choice modes; this is now encapsulated in _extract_tool_calls. The new test file provides good coverage for various combinations of reasoning and tool parsing, including edge cases like named tool choice with None content.

---

